# Archived Python prototype

This directory preserves the initial orchestration prototype for historical reference only.
It is not part of the current application runtime, build, test, or release process.

The production implementation is the Go backend under `backend/` and the React/TypeScript frontend under `frontend/`.
