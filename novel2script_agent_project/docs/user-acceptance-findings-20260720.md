# 用户验收问题记录（2026-07-20）

当前阶段：全局 12 步流程第 11 步“用户验收与发布候选观察”。

## 已修复并回归

### UAT-20260720-01 `script_context` Trace 操作名错误

- 状态：已修复并回归。
- 优先级：P2，可观测性问题，不阻断生成。
- 复现 Run：`run_001461`。
- 实际行为：内部 `script_context` 准备任务在 `llm_trace.jsonl` 中记录为 `operation=write_script`、`artifact=script_context`。
- 预期行为：操作名应明确表示准备剧本上下文，例如 `build_script_context`，避免被统计为剧本正文生成。
- 影响：不影响 Artifact 和用户流程，但会污染操作分类、节点耗时统计和日志诊断。
- 非小说复现 Run：`run_001599`，同样记录为 `operation=write_script`、`artifact=script_context`，确认是双链共性问题。
- 修复结果：`script_context` 统一记录为 `operation=build_script_context`；剧本正文 `script_unit` 仍记录为 `operation=write_script`，原有任务关联 ID 保持不变。
- 回归结果：Worker 定向测试、后端全量测试 `go test ./...`、静态检查 `go vet ./...` 全部通过。

## 已记录，暂不修复

### UAT-20260720-02 “故事开头扩写完整故事”首次被误判为小说改编

- 状态：已确认，待后续修复。
- 优先级：P1，主控来源模式识别问题；本次因缺少生成配置而未启动错误链路。
- 复现作品：`project_a9f26b0b366cfc7c1c32b917`。
- 用户原始表达：要求读取 TXT，根据现有情节设计完整剧本，并明确“当前的 TXT 原文只是一个开头”。
- 实际行为：主控首次返回 `generate_from_novel`；用户再次说明“把 TXT 当作一个开头故事大纲”后才纠正为 `generate_from_material`。
- 预期行为：当用户明确要求在不完整开头或故事大纲基础上补写完整故事时，应优先识别为非小说素材扩写；如果仍有歧义，应追问是否允许新增剧情，而不是默认小说忠实改编。
- 影响：如果用户同时给齐生成配置，存在直接启动错误链路的风险；小说链会强调忠实原文，无法正确承担补全完整故事的任务。

## 已知但按用户要求暂存

以下问题本轮不修复，等待本次小说与非小说实测结束后统一处理：

1. 剧本选区“删除”会因空 `new_text` 被 Worker 校验器拒绝。
2. 上述失败只向用户显示通用“任务执行失败”，缺少可执行原因。
3. 约 92 字的剧本局部删除仍向内容模型发送约 12,282 字符输入，局部修改上下文偏大。
4. 个别单集初次生成加长度修正的累计等待超过 100 秒，属于性能风险，尚未形成单次调用超时。
5. 非小说单集剧本请求约 42K 至 47K 字符，接近当前 48K 上下文预算上限；与局部修改上下文偏大属于同一类上下文编译效率问题。

## 已验证正常

- 小说链 9 集拆分为 10 集后，配置、Artifact 版本和下游批次同步正确。
- 第 1 集局部新增仅更新目标单集，其他单集版本不变。
- 失败删除没有生成错误版本或污染既有内容。
- 后续新选区修改成功后，第 1 集升至 v3，聚合 `scripts` 升至 v2 并保持 10 集。
- 非小说 Run `run_001599` 完整生成 6 集：素材库、故事种子、剧集蓝图、分集卡、分集剧本 5 个确认点正确，`script_context` 未暴露确认卡，0 失败、0 重试。
