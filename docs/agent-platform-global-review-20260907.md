# Agent 平台全局 Review

日期：2026-09-07。范围：当前未提交工作区，不是 HEAD 或用户运行环境。

## 结论

2026-09-14 G4.110-G4.111增量：生成阶段缺少原SDK来源及冻结/恢复合同，现补原生rollout序列化、三身份绑定、Go私有存储及有界内部HTTP回执校验；拒绝错来源、损坏/未知回执替换，来源遗忘清空正文且撤销初始未读记忆的旧执行。来源HTTP不跟随重定向或隐式代理，错误不暴露原文。来源52项、传输69项通过，Go build/vet通过但行为未验；SDK全量结果见Goal进度。来源归档不是生成执行，持久提取/整合调度、完整重启恢复及真实联合仍未完成，最终全局review未签收。

2026-09-14 G4.109增量：发现私有记忆仅保存审批参数哈希，用户无法在批准前确认写入内容。现增加私有原参数/工作区会话绑定与本人no-store预览，和实际发布共用快照读取校验；批准事务拒绝已修改/遗忘/缺快照的候选，失效记录保留拒绝入口，不返回旧正文。前端专用审批卡校验原身份/审批subject并逐文件展示变更，保留未知响应的原操作。专项38项、TypeScript和Go build/vet通过，完整前端结果见Goal进度；Go行为与真实联合未验，Schema68未迁移。自动提取/整合生产调度、跨会话恢复、快照隐私清理及最终全局review仍未完成。

2026-09-14 G4.108增量：修补私有记忆生成文件无法持久保存的工具链缺口。Go原生快照发布必须绑定原执行、本人的审批、原参数和CAS版本，持久回执之前不能完成工具；发布者自己的确认写入可以保持原输入版本收尾，用户修改/遗忘仍撤销旧执行，含新建记忆场景。SDK三模式接准备/保存工具，严格回执且禁止自动重试；共享工具摘要隐藏私有路径/正文。末版SDK1724项通过，Go build/vet通过但行为未运行，Schema67未迁移。尚不是自动提取/整合调度、隐私清理或真实联合验收完成，最终全局review仍未签收。

2026-09-14 G4.107增量：Memory管理存储/页面、执行版本撤销及三模式原生读取已补源码。SDK Memory读取原生工作区中的私有版本文件，不以规则或Session替代；manifest仅保留第三类来源引用，Go按执行用户读取。前后端禁止直接将私有目录发布为共享文件。末版SDK全量1703项通过、Go build/vet通过，但Go行为与真实模型/OCI/用户页面联合未验。SDK自动生成/持久保存、旧快照隐私清理仍未完成，不能据读取接线宣称完整Memory或最终全局review通过。详情及证据见Memory专项和Goal进度。

2026-09-14 G4.101：确认原Sandbox Memory仍未进入生产装配。固定SDK默认生成另建Runner/配置并在关闭钩子执行，不能仅追加类名即满足平台工具审批和持久交付；工作器异常存在日志吞掉路径，不能据关闭返回签收。证据及完整接入待办登记于`agent-platform-memory-integration-audit.md`。本轮只读核查和无网络配置探针，无生产修复或新行为验收；不是独立全局review完成。

2026-09-14 G4.100增量：修复项目Goal已持久化但前端不展示的接线缺口。工作台消费当前快照，显示目标标题/完成条件/版本，严格匹配作品和会话，结束后由权威null清除；不新建执行循环或将Goal视为Memory。新增5个App级替身用例，扩大工作台/事件225项及TypeScript通过，36.43秒，进程终态。尚无浏览器/真实模型Goal全生命周期证据，本版未跑前端全量；最终review仍未签收。

2026-09-14 G4.99增量：W5配置入口错误将none视图强制绑定上传材料，输入Schema明明无需材料仍无法保存。已复现并修复，none使用冻结Schema判定，缺失Schema及真正要求材料的情况保持拦截；同步改正剧本专用标题。两个专项文件79项、TypeScript和完整前端66文件986项通过，0失败/错误/跳过，254.60秒，所有进程终态。组件/API替身证据，不签署真实用户整链路或最终review。下一核对项目Goal的对话操作与可见反馈，前端快照字段无显示消费是待核对证据，不提前作整体未实现结论。

2026-09-14 G4.98增量：取消/未知结果的Go事务门禁源码核对后，修复前端人工核对回执缺少完整事实比对。六个错配反例修复前失败，现校验结论/依据/确认人及工具/参数/配置/执行归属；失败保留表单和原请求重试。前端专项19项与TypeScript通过，全部进程终态，无Go或浏览器联合证据。W4本轮所查链路未另定位代码缺陷，下一项W5；不据此签署整组真实验收或最终review。

G4.97末版验证：完整SDK1663项通过、0失败/错误/跳过，276.399秒，进程终态；下条“运行中”已由此更新。当前只证明固定SDK与测试夹具范围的回归，真实Go/网关/OCI和最终全局review仍未签收。

2026-09-14 G4.97增量：W4参数缓存风险已复现。Go已有原ID冲突门禁，Sidecar却在命中缓存时直接返回旧审批，非脚本工具变更参数未拦截。已给共用入口增加工具ID/规范JSON参数哈希，旧缓存重新向后端读取权威回执；16个修复前失败反例及原脚本专项合计38项修复后通过。完整SDK回归运行中，未据此签署完整W4/最终review，未部署或改变用户数据。

2026-09-14 G4.96增量：修复Hosted图片及代码文件保存回执未传入当前project_id/source_type的遗漏，现在与MCP同样核对资产来源和当前工具调用。八个错配反例修复前失败、两个合法对照通过；末版工具交付79项通过，0失败/错误/跳过，34.095秒，含本地stdio/模拟Responses，非真实Go/网关/OCI验收。下一核对点为非脚本工具缓存命中时的参数一致性，尚待反例验证；W4和最终全局review未签收。

2026-09-14 G4.95增量：W2作者包和未知回执源码核对后进入W3。确认后台结果外壳允许额外字段被Pydantic静默忽略，错位正文可能消失但任务仍完成。四个实际Worker反例修复前失败；修复输出/产物外壳为extra=forbid，保留开放result/payload。末版后台四文件34项通过，0失败/错误/跳过，7.098秒；含固定SDK审批、暂停、追加恢复，模型/后端为替身，非真实联合验收。临时目录权限错误经同目录审批重跑解决，未改安全策略。W2/W3当前核对及缺证据已写入结项清单，下一项W4；最终review与同版全量仍未完成。

2026-09-14 G4.94增量：G4.93完整SDK日志及JUnit已核实为1632通过、0失败/错误/跳过、279.224秒，上次回归已终态。W2检查发现ZIP/目录导入只登记完整文件路径，未拒绝`assets/Logo/a.txt`与`assets/logo/b.txt`的父目录别名，会在不同文件系统形成不同目录结构；文件与隐式目录同路径也未在预检拒绝。共享登记器已补目录身份和冲突校验，新增十个ZIP预检场景源码，Go行为待验。安装原回执、不可变包和事务切换仅完成本轮源码阅读，W2及最终全局review不据此签收。

**本轮已开展跨模块代码 review 并修复具体问题，但完整能力验收不通过，不能发布“SDK 核心功能已全部可用”的结论。**

2026-09-10 G4.93增量：同名不同ID脚本快照改按capability ID保存，工具新增兼容的确切ID参数；旧名称仅在name/script组合唯一时允许，参数不补写到原审批哈希。Go登记、重试和执行检查参数身份与固定快照一致。新增反例修复前11失败/2通过，初修21通过，扩大9文件218通过；完整SDK回归正在运行。Go全后端build/vet通过，新增Go行为用例未执行。前端/schema/依赖未改，未部署；真实环境和最终review仍未过。

2026-09-10 G4.92增量：W1接线核对后修复名称提及直接选中及禁止隐式调用策略遗漏；渐进目录补路径、策略和本轮提及优先级。8文件179项回归通过；最后固定源码SDK全量1610通过、0失败/错误/跳过、256.99秒，全部本批进程终态。另确认同名不同ID脚本快照覆盖，见下面未关闭项。Go/前端/依赖未改，真实环境和最终review仍未过。

2026-09-10 G4.91增量：固定W0静态基线核对完成，真实Go/网关门禁仍未验。路由评测校验缺陷先复现20失败/3通过，修复后30/30通过；合同反例40/40通过。Stage8计划99/99不是行为执行，发布矩阵并未因此放行。下一项W1；不将本批视为完整能力或最终review完成，具体证据见Goal进度G4.91。

2026-09-10 G4.90增量：前端重新核验当前身份，覆盖焦点/路由/可见轮询和事件流撤权；原请求代次隔离、明确认证失败退出、临时网络失败保留状态。新登录即要求Cookie，首页只读写入口及旧创建/上传/重命名/删除回执跟随当前身份。末版专项22项、TypeScript、隔离Vite和四流程合同检查通过；最后固定源码全量66文件977项通过、0失败/待定、346.73秒。保留首轮972通过/3个异步等待失败、独立Skill管理82通过及修正期间975通过的记录，不将其拼成末版证据。全部本批进程终态，Go/SDK/schema/依赖未改未重跑，未部署。新增[固定原范围结项索引](/C:/Users/egois/Desktop/内容生产Agent项目/docs/agent-platform-completion-checklist.md)，后续按W0-W9收口；完整能力、真实联合及最终独立review仍未完成。具体证据见Goal进度G4.90。

2026-09-10 G4.89增量：事件流补同事务成员/归属检查和持续会话认证，失效后断流；Project通用快照通知补齐工作台能力事件刷新，返修与空闲断线轮询、明确游标重置及本地未知消息缓存恢复已接。新增五个Go顶层用例和G4.88不可变事件夹具修正只有源码未运行，末版全后端build/vet通过，不据此关闭授权P1。末版前端专项226、全量66文件956项通过，0失败/待定、338.22秒，TypeScript、隔离Vite及四流程合同检查通过。连续刷新缓存反例先失败再修复；之前955项全量不冒充末版结果。全部本批进程终态，SDK/schema/依赖未改未重跑，未部署。本批收尾不代表原清单、同版联合、真实环境验收或最终review完成。具体证据见Goal进度G4.89。

2026-09-10 G4.88增量：返修后台持久绑定实际提交/确认用户并在领取及提案/no_change提交时重查权限；SDK从持久执行身份解析作者。旧请求不推断作者，失效排队项记录失败后让出，由用户显式重新授权；原幂等回执不重绑或重排队。前端区分授权失效与模型失败。新增十二个Go顶层用例仅源码未运行，末版全后端build/vet通过；前端修复前七个反例失败，修复后专项28和全量66文件942通过，0失败/待定、263.02秒，TypeScript、隔离Vite及四流程合同校验通过。全部本批进程终态，SDK/schema/依赖未改未重跑，未部署。该P1源码已修但行为待验，不签署关闭；原完整范围与最终验收继续保留。具体证据见Goal进度G4.88。

2026-09-10 G4.87增量：审批卡已接确切产物版本选择、完整集合与快照校验，保留旧单产物/集号兼容；纠正初版把集合版本号误作成员数。审批返修HTTP只持久入队，前端不自动继续工作流，未知回执保留原目标/指令/键，临时只读恢复不丢选择。来源审批从创建事件投影，托管自定义产物以冻结输出合同进入对话候选。新增七个Go顶层用例与扩展HTTP检查点用例仅源码未执行；末版全后端build/vet通过。前端扩大专项242、末版全量66文件934项通过，0失败/待定、310.02秒，TypeScript/隔离Vite/四流程合同校验通过，全部本批进程终态。SDK/schema/依赖未改未重跑，未部署。新确认P1见下节：后台revision Worker没有持久请求作者，真实无身份执行路径未覆盖权限复查；不是外部环境阻塞，必须继续修复，不能将本批计为完整返修交付或最终review完成。证据见Goal进度G4.87。

2026-09-10 G4.86增量：托管Skill单产物、逐集和多产物的人工保存/返修提案/接受修改现复验同一冻结Schema。沿当前版本的生成祖先读取合同，不跟随可变attempt或用旧合同掩盖最新合同丢失；逐集绑定episode_no，多产物替换目标后验证完整对象。返修开始在Go事务内冻结实际Schema及重算hash，Sidecar将其传给SDK；最终补丁完成后校验，no_change不伪造修改。完成回调补当前授权、最新尝试和基线复查。另修正G4.85遗漏的多产物ContextPack领取门禁，以及单产物重生成未更新步骤指针。新增六个Go顶层用例与合法SDK主体完成断言只有源码；末版全后端build/vet通过。SDK最后1552通过、0失败/错误/跳过、369.66秒；前端返修/保存/审批专项52项、TypeScript及原四流程合同检查通过。本批没有Go事务/Sidecar HTTP或真实模型联合运行证据，全部进程终态，未部署。下一项为审批卡通用目标选择缺口，不以本批格式校验替代完整用户返修闭环；原完整范围和真实验收未完成。证据见Goal进度G4.86。

2026-09-10 G4.85增量：原可声明的单任务多类型one输出已接通完整对象Schema、Go原子保存/依赖、全部原版本回执、集合审批及整组重生成，不在第一份输出提交时提前切换重生成状态。singleton审批绑定原step任务，次要产物编辑不占主指针，旧剧本交接保护保留；多产物人工修改复验原冻结合同和整组Schema，原提交重试仍返回原模型版本。SDK托管输出不静默删除未声明字段。新增六个Go顶层测试只有源码；最终全后端build与全量vet通过。SDK专项205项，随后全量1531项通过、0失败/错误/跳过、292.15秒；前端集合确认30项、TypeScript及四原流程合同检查通过。完整SDK使用替身模型/服务，不是真实网关/Go事务验收；前端生产代码未改，全部进程终态，未部署。下一项继续已发现的单产物人工保存缺少冻结Schema复验，并核对Agent返修/接受路径。原完整范围及同版真实环境验收仍未完成，不签署最终review。证据见Goal进度G4.85。

2026-09-10 G4.84增量：托管Skill直接产物现同时满足provider与artifact Schema，ContextPack给SDK联合Schema、Go提交复核两者，旧冻结合同保持不变但同样双检。无审批原诊断已纠正：model/batch直接confirmed本就被编译器禁止，本批仅拒绝pending_approval与无审批等矛盾声明；不自动确认模型输出。SDK生成进入JSON兼容模式后，输出修复不再重置structured，暂停及重建保留原模式；修复前专项114通过1失败，扩大恢复模式反例6通过3失败，修复后八文件193全部通过。使用真实固定SDK Runner与模型/后端替身，不是真实网关或Go事务验收。新增三项Runtime及两项Capability顶层Go行为源码未执行；本批最后全后端build、十包vet及原四流程合同校验通过。前端未改、未重跑全套分层测试，全部进程终态，未部署。不同类型多输出仍可注册但通用提交/审批/重生成未实现，是下一项代码缺口；原完整范围和同版/真实环境验收未完成，不能承诺最终review交期。详细证据见Goal进度G4.84。

2026-09-10 G4.83增量：修正上一条定位，托管Skill原上传校验已拒绝全部batch，不是可安装包仅缺启动。本批接入现有episode_no逐项协议的串行/并行声明、首步规划、包内直接产物Schema和版本集合审批；保存分集的资产/配置/决策依赖，目标集数服务端限制1至10000。其他批处理协议仍不支持，不能据此声称任意Workflow可执行。新增两项Capability及六项Runtime顶层用例只有源码；末版全后端build和十包vet通过。固定SDK Runner加模型/后端替身专项107项通过，前端通用批量确认专项29项和TypeScript通过；原四流程合同检查通过，新包另有JSON Schema检查，未进行真实Go/SDK联合。测试包只在fixtures，未安装或部署，全部进程终态。继续检查可选响应格式、多输出及无审批声明与提交入口的契约一致性；原完整范围和真实验收仍未完成。详细证据见Goal进度G4.83。

2026-09-10 G4.82增量：复核确认未注册执行器已有动态可用性门禁，本批修复的是实际调度不一致。Resume现按事务内Run状态逐步处理Runtime后继，不把Runtime排入Worker队列；待审批/真实完成即停，已开始的质量审核不重复规划或因running被回滚。循环在同名Runtime步骤的新pending实例让出并保留幂等回执。四个Runtime执行器传播真实推进结果，run.completed由共同终点提交处发出，不再在中途剧本聚合后无条件报告完成。新增五个Runtime顶层测试源码覆盖链式交接、终点、审批、循环、重开/重试、故障回滚及聚合分支；全部未执行。末版全后端build、十包vet及原四流程37步骤/21适配器合同校验通过，本批进程终态；前端/SDK/依赖/schema未改未重跑，未部署。下一项为动态Workflow首步batch被启动门禁拒绝及完整批量执行协议核对，不以只放开入口代替用户交付。原范围及真实验收仍未完成。

2026-09-10 G4.81增量：补Go清单编译器及Node合同校验的注册前条件检查。保留原十种已知条件，未知名称、同条件不同目标、failure自环和额外对象字段拒绝；user_stop仍待语义确认，作为已知但不可用条件明确拒绝，不算实现。保留字符串completed简写、同目标条件及已编译快照When/To读取。修复前六种非法清单被旧Node校验器接受；末版完整Node回归40通过、0失败/跳过/待定、87.14秒。新增五个Go顶层用例未运行，末版全后端build和九包vet通过，不能代替行为验收。前端/SDK/依赖及数据库schema未改未重跑，manifest校验schema增加枚举，未部署。下一项核对动态Workflow清单与调度入口的一致性，尚未认定新缺陷。原范围及真实环境验收仍未完成，不能据批次数或测试数推算review完成比例或交期。

2026-09-10 G4.80增量：已补状态化最终失败的failure后继选择，重试和整批结算优先；原失败/SDK checkpoint/已完成工具结果保留，新后继仅pending并经显式继续启动。后继使用冻结来源输入和确切失败事实，未知写入、未结束PTY、保护失败、重生成计划及输入/写锁冲突不跳转；恢复前重验决策hash和事实，非法分支不吞掉失败报告，存储故障整体回滚。前端过程记录区分失败后续待继续/不可继续，HTTP状态冲突409。新增七个Runtime顶层测试和扩展HTTP用例均未执行；末版全后端build、九包vet通过。前端专项78通过，最后全量65文件900通过、0失败/待定、268.80秒，TypeScript、隔离Vite与四流程合同校验通过，本批命令全部终态。SDK/schema/依赖未变未重跑，未部署。下一项为清单编译器仅校验when非空导致未知条件可注册的已确认缺口；user_stop语义仍未获答复，保持彻底取消。完整原范围及真实验收门禁未完成，不签署最终review。

2026-09-10 G4.79增量：user_continue通过现有持久审批/决策/版本集合接入普通推进、Runtime和质量审核；创建请求不是用户已继续，批准确切输入/目标/版本后才产生事实。普通Resume不能跳过，取消仍不执行后继，重复批准按原回执返回。新scope=workflow_transition通过服务端registry显示通用确认，不再误显示扩写策略。新增四个Runtime及一个投影Go顶层用例未执行；最后全后端build及九包vet退出0。前端专项33通过，最后全量65文件898通过、0失败/待定、232.73秒，TypeScript和四流程合同校验通过，全部本批进程终态；SDK/schema/依赖未改未重跑，未部署。user_stop含义已异步询问未答复，failure边仍须处理整步结算/自动重试/副作用风险。完整原范围和真实验收门禁保持未完成，不签署最终review。

2026-09-10 G4.78增量：不完整材料条件已接封存缺集政策、当前部分成功审批和参考剧本聚合。删除Run级最新部分决策读取，改为当前来源步骤、任务状态/成功数/失败键、决策hash、唯一原审批事件及当前获批版本集合校验；补齐后不继承旧失败标记，仍缺新输出的旧聚合输入拒绝。编辑待确认部分结果时，同事务重新关联新决策/新审批并保留缺集说明，必须显式批准新版本；不沿用旧批准。新增四个Go顶层测试仅源码，未执行，修复与边界仍须行为验收；最后全后端build和八包vet退出0。前端/SDK/schema/依赖未改未重跑，未部署。原user_continue/user_stop/failure条件仍未接推进事实，四流程完整交付、W0至W9/GAP及真实Go/OCI/网关/浏览器/回滚验收保持未完成，不签署最终review。

2026-09-10 G4.77增量：四原流程后继核查发现推进器忽略when而取首条，现成功/审批路径按Runtime事实选择，批次全部成功必须有真实非空任务集合且全部succeeded；无匹配或歧义返回409并回滚。普通/整步/单选/改编/扩写/重生成回接及质量审核已接，模型不能自由决定Step。新增六个Go顶层测试只有源码未运行；全后端build和八包vet通过。原四清单37步骤/21适配器校验通过，Node合同回归首轮spawn EPERM，经正常同命令审批后32通过、62.87秒，不能冒充新增Go条件行为通过。前端/SDK/schema/依赖未改未重跑，全部进程终态，未部署。原合同仍有user_continue/user_stop/incomplete_material_confirmed/failure四种条件未接推进事实，明确为代码缺口，不全部推给环境。继续原四流程完整交付、SDK三模式/W0至W9/GAP及真实Go/OCI/网关/浏览器/回滚验收，不签署最终review。

2026-09-10 G4.76增量：修复分集确认的部分成功缺陷，模式快照与确切版本审批进入同一ResolveApproval事务，前端不再先独立修改模式。旧空payload/null/空对象兼容，非法模式和错误审批scope拒绝；模式事件及审批resolution记录确切配置快照。独立模式API复用现有当前授权及原回执目标核验。确认后显式继续仍是独立命令，不宣称确认加继续原子。新增Go成功/重开恢复/失败回滚和授权用例均未执行，只通过全后端build及七包vet；初次vet夹具错误已修正。修复前两个App反例失败，专项最后200通过；最后固定前端65文件897项通过、0失败/待定、272.69秒，TypeScript与隔离Vite通过，所有进程终态。SDK/schema/依赖未改未重跑，未部署。App加mock API不能代替真实Go事务或三模式联合；四流程完整交付及原W0至W9/GAP仍须收口，真实Go/OCI/网关/浏览器/回滚验收仍未通过，不签署最终review，也无可核定的整体剩余小时承诺。

2026-09-10 G4.75增量：前端公共消息类型收敛为AgentTurn，删除旧MessageExchange同步回执后补素材、配置和注入结果的编排；旧协议不再触发后续写入，保留原请求恢复。修复直接返回已完成回执时遗漏最终回复/配置投影，以及选Skill发新消息前自动取消旧失败Run。用户主动配置/启动仍保留，失败Run写锁由后端既有确认启动事务释放。四流程各覆盖完成事件、断线轮询和已完成回执，但仅为实际App加mock API证据，不是SDK/Go联合或完整交付验收。扩大专项316通过；首轮全量888通过1失败，修正Skill管理离页警告用例的异步等待后，最后固定前端65文件894项通过、0失败/待定、243.78秒，TypeScript和隔离Vite通过，全部本批进程终态。Go/SDK/schema/依赖未改未重跑，未部署。继续原四流程后续交付和W0至W9/GAP逐项核定，真实Go/OCI/网关/浏览器/回滚门禁仍未通过，不签署最终review。

2026-09-10 G4.74增量：消息接受入口已增加事务授权及只读原回执恢复，在当前Skill/素材校验和新请求开关前核对已有执行；新消息hash固定于自动素材补齐前并带版本标记，旧协议仅按原不可变接受回执核对，不重放入队/取消/排队编辑，不重新计算当前素材。前端恢复不先取消新失败Run，错误归属/状态/格式回执不清原记录，恢复4xx保持未知，停止后迟到成功不覆盖新提交；WebCrypto不可用时不声称已有原键级前端核对。专项首次185通过24失败，修正不完整回执夹具和用例隔离后209通过；最后固定前端全量65文件877通过、0失败/待定、218.78秒，TypeScript、隔离Vite、Go build及七包vet通过，全部进程终态。新增Go行为源码未运行，未进行实际浏览器/SDK/Go联合或回滚验收。SDK/schema/依赖未改，未部署；新hash格式仍须配套回滚验证。下一项按原W5/W9核对并收敛前端旧MessageExchange同步返回后补素材/配置的流程分支，不能以旧协议夹具替代当前AgentTurn/SDK完整链路。原四流程、完整能力清单与真实环境门禁保留，不签署最终review。

2026-09-10 G4.73增量：四流程首条及普通消息已接按工作区/用户/作品/对话隔离的sessionStorage原请求恢复，保留确切Skill版本、附件/批次、上下文、原键和客户端ID；先记录pending后发送，重建不自动重发未知请求，停止/迟到结果不删除后续记录，存储失败锁写，已确认后的清理只操作本地。最后固定前端全量65文件860项通过、0失败/待定、214.98秒，TypeScript和隔离Vite通过，所有进程终态；组件重挂载与实际App加mock API不等于实际浏览器/Go联合验收。继续复查确认后端createMessage在原回执查询前校验当前Skill/素材/选区，且自动补素材后才计算请求哈希，已接受消息的重试仍可能被当前变化拦截；这是未修复代码缺口，不是环境问题。下一步须保留当前授权及旧键防重复边界修复该链路，不能以客户端保留原键宣称恢复完成。Go/SDK/schema本批未改未重跑，原四流程、完整能力清单及真实环境门禁保留，未部署，不签署最终review。

2026-09-10 G4.72增量：Skill管理八类命令已增加IndexedDB原请求恢复，绑定工作区/用户、原快照/目标/键及ZIP真实字节和元数据，先持久claim后发HTTP；重建只展示，不自动提交，不覆盖并发页面记录。修复跨realm二进制验证、另页清理后显式刷新无法解锁、角色变更误拦纯本地清理和sessionStorage写回失败丢失已读取客户端ID。扩大回归先79通过6失败，修复后专项106全部通过；最后固定前端全量64文件831项通过、0失败/待定、250.66秒，TypeScript和隔离Vite通过，全部进程终态。新增fake-indexeddb仅开发测试依赖，SDK/Go/schema未改未重跑；组件重挂载不是实际浏览器重载或持久磁盘验收。四流程共用首条消息交接仍在发送前删除持久记录，后续参数/键仅在内存，须继续修复重建恢复及核对精确版本/身份绑定，保留StrictMode防重复提交。原四流程、完整能力清单及Go/OCI/网关/浏览器/回滚门禁仍未全部通过，未部署，不签署最终review。

2026-09-10 G4.71增量：ZIP安装/升级与目录纳管/更新已接既有安装事务和持久命令回执；升级事务内复查完整观察快照，新安装不覆盖已安装同名包。旧键核对原审计回执后读取当前状态，不重放后续升级/卸载/重装，也不要求原目录仍存在。前端保存原File/参数/键并校验ZIP原字节哈希、范围/版本/事件；未知结果可只读刷新及同页原请求恢复，成功但刷新失败只重读。修复完成诊断被后续读取错误改成失败、状态更新零行仍继续提交的问题。最后前端62文件790项通过、0失败/待定、221.51秒，TypeScript、隔离Vite、Go build和七包vet通过；新增Go行为源码未执行。SDK及schema未改，未重跑SDK，全部本批进程终态。整页重载恢复尚未实现，原四流程、W0至W9/GAP与真实Go/OCI/网关/浏览器/回滚验收门禁保留。未部署，不签署最终完成。

2026-09-10 G4.70增量：启停、版本切换及卸载已接用户观察快照与持久幂等回执，复用安装事件和既有幂等表；当前身份/范围、原请求哈希、确切审计事件及事务内版本/状态/启停/事件数共同校验。旧回执只能重读，不会覆盖后来升级、卸载或重新安装的状态。管理页冻结原快照/原键，校验回执与包/事件归属，未知结果允许只读刷新和原请求恢复；后续撤权不冒充原操作失败。整页重载持久恢复未实现。新增Go Runtime/HTTP行为源码未运行，仅build及七包vet通过。初轮前端全量767通过期间仍有源码修改，不作最终同版证据；最终固定源码61文件768项通过、0失败/待定、204.62秒，TypeScript与隔离Vite通过，所有进程终态，SDK和schema未改未重跑。旧ZIP安装/升级、目录纳管/更新仍缺同等持久命令与事务快照保护；原四流程、W0至W9/GAP及真实Go/OCI/网关/浏览器/回滚门禁保留。未部署，不签署最终完成。

2026-09-10 G4.69增量：修复Skill版本选择自动重新启用已禁用安装，受支持版本保留原启停状态并同步注册快照。新增Runtime和HTTP行为用例未运行，仅Go build及七包vet通过。管理页补同步提交锁、角色/项目变更及卸载页面后的迟到响应隔离、失败后旧写入口失效、已提交但刷新失败的独立只读重读、ZIP选择目标冻结及目录预览确切版本校验。最后前端61文件750项通过、0失败/待定、212.55秒，TypeScript和隔离Vite通过；SDK及固定依赖未改未重跑。旧ZIP/回滚/启停/卸载仍缺完整用户观察快照CAS与持久幂等回执，页面保护不等于未知结果恢复完成。全部本批命令终态，未部署或运行受限Go测试。原四流程、W0至W9/GAP及真实验收门禁保留，没有可信的剩余小时承诺，不签署最终review通过。

2026-09-10 G4.68增量：核定原Skill作者/校验/安装和当前执行目录接线，补工作文件窗口中原安装状态跨文件事件/关闭重开的保留、范围和回执版本校验、旧安装版本非当前可用提示，以及目录刷新失败后独立重读。完整App夹具验证安装后Skill进入菜单并发送准确版本；修复实际工作台吞掉刷新失败和Composer继续使用过期Skill选择的情况，原未确认发送仍允许原版本/原键重试。最后前端61文件738项通过、0失败/待定、197.97秒，TypeScript、隔离Vite、Go build和七包vet通过；Go行为未运行。同版固定SDK全量经现有依赖读取审批后1507项通过、0失败/错误/跳过、309.04秒，未升级SDK或修改系统策略，本批全部进程已终态。Skill管理页旧ZIP升级/回滚/启停/卸载提交边界是下一项明确复查，不以草稿安装闭环替代整个Skill生命周期；原四流程、W0至W9/GAP及真实环境门禁保留。

2026-09-09 G4.67增量：继续附件选择/批次/运行输入链路。前端补移除、排除/纳入和重新收集，保持已使用封存批次不可重开的后端边界；ZIP子文件移除后解除整包引用，切换Skill删除材料不再复用旧批次。仅纳入成员进入视频输入，通用混合材料保留其他文档而不自动绑定纯视频批次。多文件部分上传成功可保留，新建作品可移除已上传附件，未知批次操作关闭后仍能原键重试；成功回执在父组件同步失败时继续保留。后端补集合/版本确切配对及运行输入错误。专项81项通过、19.94秒，TypeScript、隔离Vite、Go build及七包vet通过，Go新增行为未运行。首轮全量700通过/1失败的发送门禁等待已修正；最后同版全量61文件703项通过、0失败/待定、209.29秒，所有本批进程已终态。原四流程、Skill闭环、W0至W9/GAP与真实环境门禁未完成，不宣称最终review通过。

2026-09-09 G4.66增量：同Run新聚合改为独立候选，不再更新原最终稿；完成后编辑保留未修改正文的确认状态。schema64迁移源码保留原候选及定稿/预览/导出引用，含失败回滚和外键检查，未执行用户数据库迁移；不能声称已恢复此前可能被旧代码覆盖的历史稿。新增完整App测试发现新版切回旧候选时正文被单向版本保护拦住，现以显式候选版本区分阅读工作区，普通刷新和脏稿保护不变。首轮138通过/1失败；修复后专项139项通过、43.75秒。Go全后端build和七包vet、TypeScript及隔离Vite通过；Go完整编辑/聚合/重选及迁移用例仅源码，行为未运行。最后前端同版全量60文件674项通过、0失败/待定、198.40秒；原四流程、附件输入一致性、Skill闭环、W0至W9/GAP和外部验收仍须完成，不签署最终review。

2026-09-09 G4.65增量：素材批次创建/修改/封存/重开补事务内当前授权、版本/持久回执与CAS检查；修复排除未识别成员后的空指针、全部排除仍可封存和超大集号导致无界缺集枚举，集号明确限定1至10000。前端补同页关闭/重开后的原操作恢复、只读/目标校验、成功后仅重读、未提交集号保留及继续上传到原批次。候选远端推进后必须显式重选版本；恢复G4.64误拦的历史最终稿重选能力。最后前端60文件673项通过、195.96秒，TypeScript、隔离Vite、Go build和七包vet通过，Go行为未运行。**历史最终稿数据独立保留仍未完成**：完成后编辑重开原Run，聚合仍更新该Run的同一候选；须继续修复编辑期间及再聚合后的旧稿保留，不能仅以UI门禁替代。原四流程、Skill闭环、完整W0至W9/GAP及真实环境门禁不缩小。

2026-09-09 G4.64增量：候选预览/最终确认/导出增加事务内当前授权、精确版本与持久回执校验；历史合集阅读改为加载合集保存的单集版本引用，修正最终确认前端响应类型，下载核对字节与hash，补原键重试和脏稿保护。最后前端全量58文件646项通过、0失败/待定、193.93秒；此前640通过/6失败的日志保留，修正新用例的异步提交等待后重跑全部通过。TypeScript、隔离Vite、Go build和七包vet通过；Go新增行为用例未运行。候选远端推进后预览与操作目标一致性、素材批次确认仍待核定/修复，原四流程、Skill闭环和完整W0至W9/GAP及真实环境门禁保留；不据此签署最终review完成。

2026-09-09接续至G4.63：本文件下方2026-09-07发现及证据保留为历史，不将其中的“尚未实现”直接当作当前源码状态。当前剩余交付、后续补接和验证边界以[Goal进度](/C:/Users/egois/Desktop/内容生产Agent项目/docs/agent-platform-goal-progress.md)顶部清单及批次记录为准。G4.55至62下载、审批、保存、接受事务、交接恢复、运行/后台/主轮控制、启动和工具审批修复保留；G4.63补返修目标/执行事务授权、幂等入队、期望版本与回执目标绑定，修正旧失败回调及超时遗留恢复，前端增加原键重试和跨视图待确认操作保护。复查中纠正了本批初版将SDK合法执行误当成用户确认而阻断的问题，现分开核定主体，未替换SDK模型循环。最后前端56文件617项通过；当前后端build及七包vet通过，但新增Go行为用例未执行，SDK未改未重跑，不替代真实联合。固定写入口清单剩候选导出/定稿预览、素材批次确认两组，另继续原四流程和完整W0至W9/GAP；真实环境验收和最终独立review未通过。

原顺序是功能达标、全量验收、最终独立 review。复核发现第一步仍有实质缺口，因此本文件是当前代码的跨模块审查及回归记录，不冒充完成原目标后的最终验收，也不是另一个独立审查者的签字。继续保留原能力基线；不把无法完成的功能静默移出范围。

未访问、重启或升级 8860/8880；未修改用户作品、数据库、网关、模型或凭据；未 push、部署、访问测试机、安装 OCI、修改系统策略或重置额度。

## Review 发现与处理

### P2 同名不同ID的Skill脚本选择丢失（G4.93源码已修，Go行为待验）

- Registry按capability ID选择条目，不同作用域可分别安装同name而不同ID的扩展包；`_selected_skill_scripts`却以skill.name索引快照、以(name,script_id)保存允许集。同轮选中两者时，最后一个包覆盖前者快照，`ensure_call`据此绑定后者，SDK工具参数仅有skill_name/script_id，无法表达确切选择。
- 当前函数只读探针已复现：scope_a和scope_b的name都为same-name且含review，最终快照只有scope_b。此证据是Python实际选择函数，不声称已执行真实Go或OCI错误脚本；Go后续确切版本校验不等于上游选择正确。
- G4.93按capability ID分别保存脚本许可与快照，SDK工具接受确切ID；旧名称仅在name/script组合唯一时兼容，未知ID、未选中ID、错名称/脚本及歧义均不登记。登记后的原参数不因动态激活或升级重绑，Go独立核对显式ID、invocation与原审批快照，不按Registry顺序任选。原参数省略ID或显式空ID均保持原哈希语义。
- 修复前11失败/2通过，初修21通过，扩大9文件218通过。新增同名共同/不同脚本、目录顺序、动态激活/升级和SDK审批重建用例；末版补主对话/后台/状态化身份矩阵。旧checkpoint若无明确ID且已出现歧义会拒绝，不能伪称能无损恢复其原意图。Go新增一项顶层、八个子场景仅源码及vet，未执行；真实OCI和Go联合仍未通过。挂回W1/W4，不签署整组能力关闭。

### P2 名称命中绕过意图路由与隐式调用声明（G4.92已修复，真实模型待验）

- 原`_route_skills`只要用户文本含别名即直接返回选中ID，因此“不要使用大纲诊断”也会选中；显式引用和仅解释名称未区分。当前名称只用于候选集和排序，真正选择交给固定SDK Router；输入明确菜单引用仍走原直达分支，路由错误不自动选中。
- 原动态发现接受所有可用inline包，绕过allow_implicit_invocation=false；当前共享策略过滤并校验未选中发现，任一禁止声明均保留，默认仍允许。已批准安装/升级、菜单选择、路由明确选择和后续恢复兼容。路由元数据补路径/策略，优先保留本轮提及项但仍按预算渐进披露。
- 修复前9失败/1通过，扩大8文件179通过；其中SDK结构化路由采用确定性模型，验证平台不再绕过模型决定，不证明真实模型对全部自然语言否定均正确。真实Go/网关/用户操作验收仍未通过，完整结果见Goal进度G4.92。

### P2 合成路由评测接受错误样例与虚假覆盖（G4.91已修复）

- `evals.py`原先只检查expected.intents真值，字符串会参与子串匹配；目录条目、模式/run标记、引用版本及期望/禁止能力缺乏严格校验。只引用原流程即计为覆盖，执行模式依赖自报标签，可以删除正例后用标签掩盖缺失。
- 当前严格验证字段和确切版本，四原流程与三种新增Skill模式必须有正向路由断言，负例保留但不能计为成功覆盖。修复前20失败，末版包含合法多版本/负例及三模式覆盖反例共30通过。
- 这是合成路由夹具校验，不是新Skill真实安装/执行、真实模型决策或端到端验收；不据此关闭W0/W1等整组能力。Go/前端/依赖本批未改未重跑，无用户环境操作。

### P2 前端身份停留在首次加载及旧操作延续（G4.90源码修复）

- `App`原先只在挂载时读取身份，HTTP则每次请求复查当前成员；角色变化后前端仍显示旧操作。首页创建/重命名/删除还缺少只读投影；用户切换后旧创建响应可能继续上传附件、保存原消息或把新页面导航到旧作品。不是已复现服务端越权。
- 当前按页面焦点、路由、可见性和可见状态30秒复查身份；SSE撤权强制核验。明确认证失败退出，临时网络失败不清草稿；普通读取合并，旧读取与登录结果隔离。成功登录即要求后续Cookie身份，不能因登录接口返回bearer认证元数据而漏过首次Cookie失效检查。
- 首页写入口随当前角色禁用/隐藏；原身份/权限代次失效或组件卸载后，不继续旧请求的上传、批次、重命名重试或导航。已发出的HTTP副作用不宣称被撤回；同一身份角色变更的当前输入保留，但未保证全部未提交表单跨退出登录恢复。
- 初始11个身份反例及首次Cookie失效反例有修复前失败证据，末版专项22项通过。完整回归及未通过边界见Goal进度G4.90。真实Cookie、跨租户Go事务及浏览器端到端仍待验，不能以组件替身关闭整组W7。

### P1 长连接保留旧访问权限（G4.89源码修复，行为待验）

- `event_stream.go`原先只在HTTP中间件建立连接时认证，`event.go`的游标/事件批次直接读取作品或Run，没有持续复查当前工作区成员。已建立SSE可能在用户退出、Cookie过期或成员被撤销后继续收到后续事件。这是生产调用链审查发现，不声称已运行越权复现。
- 当前每批事件及游标读取在同一只读事务检查作品归属和当前成员；HTTP长连接在开始、重放和保活时检查原身份/认证方式，Cookie失效不能退回隐式本地身份。失效后发不含正文的撤权控制并终止，前端关闭连接、移除操作、拒绝在途旧快照恢复页面。每批已经授权读出的数据不声称能被事后撤回。
- 新增Runtime权限/跨批次撤权及HTTP会话退出/过期/替换、通知帧与撤权断流用例仅源码；受限Go行为和真实浏览器联合仍待验，不据build/vet签署P1关闭。

### P2 工作台遗漏状态更新及游标重置（G4.89源码修复）

- 原事件白名单漏掉返修queued/no_change/stale等变化，断线兜底又未包含返修或空闲作品。现在保留旧事件兼容，Project流每批非纯delta事件另发通用快照变更通知；客户端只读刷新并校验作品/序号。新增能力不需再添加事件名才能触发工作台快照刷新，独立面板仍以各自数据入口为准。
- 原`stream.reset_required`没有清空SSE ID，原生重连可能反复发送已超前的游标；当前控制帧按HTML SSE协议清空ID。明确重置后新的服务端快照可替换旧本地高版本，普通刷新保护不放宽，草稿保留、未知本地消息不假报执行状态、不自动提交命令。前端专项和全量结果见Goal进度；HTTP协议字符串测试不是实际浏览器重连证据。
- 连续刷新新增反例实际暴露待确认消息缓存未同步：首次重置显示未知，下一次读取又恢复sending。已同步更新缓存并通过末版专项/全量；失败重置后重读用例保留。没有据此声称所有在途写命令响应均有epoch隔离，真实回滚和跨层事件流仍待验收。
- 另纠正G4.88几处测试夹具：只追加事件表本来禁止UPDATE/DELETE。缺授权旧记录现用临时库中的历史形态请求行模拟；损坏最新授权用新追加记录模拟，不删除触发器、不修改生产保护。G4.88从未宣称这些Go用例运行通过，当前修正后仍为行为待验。

### P1 返修 Worker 丢失提交者身份（G4.87发现，G4.88源码修复、行为待验）

- G4.87发现链：`backend/cmd/server/main.go`以普通shutdownContext启动`revisionService.Run`；poll原先直接枚举作品/请求并使用同一上下文Execute。`revision_command.go`原先仅在存在用户或Agent活动时复核主体，后续文件工作区检查同样依赖用户上下文，HTTP提交时的授权不能覆盖后台执行。
- 原`revision_requests`及创建事件未记录真实作者，来源审批ID不能替代执行身份，提交后撤权可能仍被无身份Worker执行。此发现来自代码审查，未执行受限Go程序或真实模型，不声称已完成攻击复现。
- G4.88通过同事务`revision_request.execution_authorized`事件封存作者及确切目标，不修改schema。新创建/目标确认/执行重试均记录实际用户；Worker读取最新绑定并在领取/提案及no_change提交时复查，SDK活动仍核对真实持久作者。旧请求缺失归属不猜测，失败后可由当前合法用户显式重新授权；无授权队头结算与当前版本绑定，正常等待、失败清理及原回执保持。
- 新增Runtime和Sidecar真实服务路径回归源码覆盖上述边界，但Go行为执行仍受系统限制。build/vet及前端回归只能证明各自范围，本项保持P1行为待验，不据此签署交付。

### R1 初次领取任务可能永久饥饿，已修复

`backend/internal/runtime/executor.go` 原先先取前 100 个待执行项，再筛选当前 Worker 支持的执行器和顺序依赖。前面有 100 个不适用项时，后面的健康任务一直不可领取。恢复队列的既有分页修复并没有覆盖首次领取。

改为有序扫描、完整判断适用性和顺序依赖、找到第一项后停止；不把整条队列装入内存。补 `TestFirstExecutionClaimScansPastUnsupportedQueueWindow`，构造前 101 项不可执行、后面一项可执行的队列，并检查前面项目没有被错误改变。

### R2 三种追加接口存在权限检查与写入之间的竞争，已修复

`agent_turn_input.go`、`agent_task_input.go`、`execution_input.go` 在进入事务前由 HTTP 认证解析角色，但只信任上下文中的角色不足以覆盖随后发生的撤权。追加写入事务内现复用 `validateExecutionOwnerTx`，检查当前成员角色及用户/工作区存续状态。旧 owner 上下文不能在降为 viewer 后继续追加。此修复仅覆盖上述三种追加命令，不声称所有历史写接口都不存在同类问题。

### R3 后台幂等重试返回过时收件状态，已修复

`agent_task_input.go` 原先命中幂等缓存时直接返回最初的 `received`，即使输入已经 `included`。现在保留不可变 input_id，读取当前权威回执，与主对话和状态化追加行为一致。补已完成任务上重试同键的回归。

### R4 Skill 发现后仍被预路由名单拒绝，已补接

`runtime.py:_load_skill_instructions` 先前拒绝未预选的 Skill，虽然 `list_capabilities` 可列出它。新增冻结的可发现 inline 目录，复用 `PreparedAgentTools.activate_skill` 和 SDK 工具装载；核对版本、内容哈希和 scope 后才加载依赖。安装、授权、执行模式和版本锁定没有放宽；未配置、禁用或越权的依赖不能因为动态发现而获得权限。

新增三个执行上下文的依赖装载测试、目录/版本/哈希/scope 变化拒绝测试，以及实际主 Runner 的发现、原生 MCP 审批、重建、批准/拒绝测试。直接函数/Provider 测试不等于三个 Worker 的完整联合验收；最终测试结果见下方。

### R5 状态化追加此前没有用户闭环，已补接

schema 48 新增 `execution_inputs`。用户 API、原作者权限、32 KiB/128 条/512 KiB 限制、幂等、领取令牌绑定、SDK 模型收件前缀及 checkpoint 校验已接。运行中追加请求原生 after_turn 暂停，并在原 attempt 上继续；不改冻结 ContextPack、不重放已完成工具。旧审批需显式继续，迟到输入保留 received，不假报模型已读。

前端新增按当前处理项选择的追加入口，复用主对话/后台编辑器；草稿存在时禁止换目标，网络失败保留内容及幂等键，SSE 更新收件。新增 API/组件/实际 App 事件测试和桌面/手机浏览器检查。

## 尚未通过的交付门禁

这些是原承诺中尚未闭环的项目，不全部归因于前端，也不全部归因于外部环境。

| 优先级 | 范围 | 当前证据与后续要求 |
| --- | --- | --- |
| P1 | 原生工作区、Shell、二进制 | `workspace_files.py` 与 `project_files.go` 是版本化文本文件桥接；`scriptsandbox/sandbox.go` 是受限单次 OCI 脚本执行。生产 Sidecar 尚无完整 SandboxAgent/Filesystem/Shell 会话接线。需要完成适配、文件/快照导入导出、隔离与恢复；真实 OCI 不可用只是验收阻塞，不能掩盖适配代码尚未完成。 |
| P1 | 用户 MCP 凭据 | `identity.go` 有工作区凭据元数据 CRUD，`agent_tools.py:_create_mcp_server` 仍从受信服务配置解析进程环境。用户凭据选择、执行消费、撤销、跨工作区隔离和对应管理入口未形成闭环，GAP-13 保留。 |
| P1 | 原生压缩与真实工具 | `session.py` 仍使用原生 SDK Session/Responses compaction，并拒绝伪 compaction；既有网关不兼容未解决。本轮没有真实网关调用，不得把确定性模型、MockTransport 或 SDK 原生类的存在算作真实模型连续对话/Hosted Tool 验收。 |
| P2 | MCP 文件交付 | `_AuditedMCPServerMixin.call_tool` 返回 MCP 原生结果；`StoreAgentToolOutput` 仍仅接受 Hosted 调用。MCP 图片/文件持久资产、下载和再利用尚未补齐。 |
| P2 | 通用协作与规则 | `skill_agents.py` 是有限的 inline handoff，现有 Task/Run 不等于通用子任务并行、汇总、取消闭环；Session 和 Goal 不等于可管理的跨会话规则/偏好。GAP-16/17 保留。 |
| P2 | 输入和失败后继续 | 当前追加仅覆盖文本；附件、已接收输入的撤回/修订、副作用失败后的结果核对与安全再执行仍需实现/验收。状态化追加历史在本轮已选 attempt 上保留，但重试后重新加载页面仅从 current_attempt_id 构造选择项，旧 attempt 的历史入口仍有缺口。 |
| P1 | 三模式同场验收 | 当前有真实 Go + 固定 SDK + 确定性模型的跨进程恢复证据，以及独立真实 App + mock API 的 UI 证据；二者不能相加宣称真实模型、后端、前端三模式同场 E2E 已通过。 |

## 已审查边界

- SDK/平台分工：Runner、RunState、add_input、原生审批、工具/Agent.as_tool 与 Session 由 SDK 执行；Go 的租约、不可变版本、事务授权、业务产物提交与幂等仍是必要平台职责，不以“完全 SDK”删除这些门禁。
- Skill：检查草稿校验、安装回执、事务复查、当前执行目录合入、动态依赖和版本恢复；没有为每个新 Skill 增加专用业务执行器。
- 状态与并发：检查主对话、后台、状态化的暂停/追加/结果修复/领取、令牌轮换、审批及副作用重试保护。
- 文件与工具：检查文本路径和大小限制、精确版本下载、Hosted 输入快照/字节检查、工具输出归属和同源下载。没有验证任意二进制/真实容器，也没有进行渗透测试。
- 前端：检查新增入口、权限禁用、草稿保护、幂等键、迟到响应、SSE 监听与卸载、desktop/mobile 不溢出。
- 兼容与发布：schema 47 -> 48 只在临时数据库验证；回滚、运行环境升级另需授权，不把源码版本当作 8860 的实际版本。

## 测试记录

本轮命令已取得终态；以下结果按代码版本与运行批次分别列出，不合并成“最终全绿”。

- G4.11b 初轮 Runtime 5 项通过，26.413s；扩展的迁移与并发测试随后通过。`.tmp/goal-g411b-input-runtime-first.log`、`.tmp/goal-review-queue-input.log`。
- Go + SDK 状态化追加三分支通过，34.126s：正常完成、输出 guardrail 拒收、完成竞争迟到输入；重建服务仍为原 attempt，工作文件版本仍为 1。`.tmp/goal-g411b-http-input-first.log`。
- 新队列测试第一版缺少 step_runs fixture 的必填列，已修正 fixture，未放宽生产 schema；新增 HTTP 权限测试第一版构造器参数及空幂等键 helper 使用错误，已修正。保留初次失败日志，不记作通过。
- 初轮完整 Sidecar 644 项通过，0 failure/error/skip，120.397s。`.tmp/goal-review-python-reviewed.xml`。这是按需发现补接之前的版本。
- 前端完整 38 文件、298 项通过；TypeScript 检查及隔离目录生产构建通过。`.tmp/goal-g411b-frontend-all-reviewed.log`、`.tmp/goal-review-frontend-types.log`、`.tmp/goal-review-frontend-build.log`。
- 实际 App + mock API 的 1440x900/390x844 追加、收件刷新、暂停、重载、继续和终态交互通过。`.tmp/goal-g411b-stateful-input-ui/report.json`，截图已查看；这是 UI fixture，非真实模型。
- 初轮 Go 全包：Runtime 716.137s、HTTP 107.684s 等 12 个有测试包通过，4 包无测试；cmd/server 因 Windows Access denied 未启动，整体退出 1。`.tmp/goal-review-go-all.jsonl`。后续源码修复不借用此结果声明同版全绿。
- 最终重跑部分 Go 程序被 Windows Application Control 拦截，包括明确的 `An Application Control policy has blocked this file`；未修改策略、ACL 或换路径规避。`.tmp/goal-review-go-final.jsonl`。
- 按需发现补接后的完整 Sidecar 首次为 646 通过、10 失败，失败集中在 stdio MCP 连接超时及其后续断言，481.69s。`.tmp/goal-review-python-final.xml`。不能仅凭集中于 MCP 就全部判定为环境原因，待单独重跑与核查。

### 收尾复核

- 最后串行 SDK 专项 85 项：82 通过、3 失败，188.43s。本轮新增的 12 项按需发现/拒绝边界/主 Runner 原生审批重建测试全部通过，既有状态化 MCP 恢复专项也通过。仍失败的是主对话作者安装后 MCP 审批的 approve/reject，以及后台作者安装后 MCP 审批，均有 `Failed to connect MCP server` 及后续工具不可用记录；保留未通过，不用其他模式的成功抵消。`.tmp/goal-review-sdk-serial.xml`。
- 三种追加的 Runtime 串行回归中，追加、权限、幂等、迁移、重建测试均通过；队列 fixture 错把有未完成前序任务的项目当作可领取，导致该批整体退出 1。修正测试前提后，首次领取超过 100 项专项及原有视频顺序失败/重试回归独立通过，18.355s。`.tmp/goal-review-runtime-serial.log`、`.tmp/goal-review-queue-final.log`。生产顺序门禁没有放宽。
- 最后 Go HTTP 权限专项与状态化追加 Go+SDK 三分支串行通过，130.644s。覆盖未登录、viewer、其他作者、跨工作区、服务冒充用户、缺少幂等键、伪造 included、旧令牌/未领取输入、重建服务、输出 guardrail、迟到输入。`.tmp/goal-review-input-http-serial.log`。
- 最后完整 Go 重跑整体失败：7 个有测试包通过、4 包无测试；5 个测试二进制未能启动（含明确 Application Control 拦截），Runtime 在 15 分钟总上限超时。最后全套 SDK tagged HTTP 重跑也失败/超时，不算完整三模式验收通过。`.tmp/goal-review-go-final.jsonl`、`.tmp/goal-review-http-sdk-final.log`。
- 字体间距调整后，UI 脚本再次退出 0，桌面/手机截图已重新查看，新增入口无横向溢出；脚本使用随机 loopback 端口、无默认后端代理，finally 关闭自身浏览器/服务。`.tmp/goal-review-ui-final.log` 与 `.tmp/goal-g411b-stateful-input-ui/report.json`。
- 最后 `git diff --check` 退出 0。通过工具返回值确认本轮所需测试命令均已结束；额外系统进程枚举被拒绝访问，因此没有声称独立验证了整机不存在其他进程，也未修改系统权限。

## 定位入口

- 初次任务领取：[executor.go](/C:/Users/egois/Desktop/内容生产Agent项目/backend/internal/runtime/executor.go:207)。
- 事务内追加权限：[agent_turn_input.go](/C:/Users/egois/Desktop/内容生产Agent项目/backend/internal/runtime/agent_turn_input.go:86)、[agent_task_input.go](/C:/Users/egois/Desktop/内容生产Agent项目/backend/internal/runtime/agent_task_input.go:73)、[execution_input.go](/C:/Users/egois/Desktop/内容生产Agent项目/backend/internal/runtime/execution_input.go:125)。
- 后台幂等回执：[agent_task_input.go](/C:/Users/egois/Desktop/内容生产Agent项目/backend/internal/runtime/agent_task_input.go:83)。
- Skill 按需发现：[runtime.py](/C:/Users/egois/Desktop/内容生产Agent项目/experiments/openai-agents-sidecar/src/content_agent_sidecar/runtime.py:616)、[agent_tools.py](/C:/Users/egois/Desktop/内容生产Agent项目/experiments/openai-agents-sidecar/src/content_agent_sidecar/agent_tools.py:240)。
- 尚未接用户凭据的 MCP 连接：[agent_tools.py](/C:/Users/egois/Desktop/内容生产Agent项目/experiments/openai-agents-sidecar/src/content_agent_sidecar/agent_tools.py:671)。
- 原生 Session/compaction：[session.py](/C:/Users/egois/Desktop/内容生产Agent项目/experiments/openai-agents-sidecar/src/content_agent_sidecar/session.py:206)。

**最终处置：本轮 review 已形成结论和修复记录；整体交付仍不通过，未标记完整目标完成，未部署。**

## 继续执行更新

用户明确不允许在此阶段性 review 后结束，已恢复完整 Goal。G4.13 补 MCP 内联媒体/文件和原生资源链接到项目资产、下载与复用，带限定身份的 Go+SDK 联跑及桌面/手机 UI 分层验证通过，详见进度文档。此更新替代上方 MCP 文件“完全未接”的历史描述，但不代表未知二进制材料、完整工作区或真实外部模型全流程已验收；本报告不是最终 review。此前三项作者 MCP 回归本次串行重跑通过，仍待最后同版全量核定。

G4.14 已补用户/工作区 MCP 加密凭据、严格内部解析、执行用户与版本绑定、撤销、实际原生连接消费，以及 Skill 管理中的入口。schema 49 的 Runtime/API/迁移/权限专项、SDK stdio/HTTP/SSE、本地 Go+SDK 三执行身份联合测试和分层 UI 通过，失败修正与证据以 goal-progress 为准。上方“用户凭据只存引用、没有执行消费”的历史发现由此更新，但操作方密钥/外部账户配置和真实模型验收仍未替用户执行。继续原剩余能力，不把此批当作全局 review 收尾。

G4.15/G4.16 已补个人/工作区/作品规则管理、对话提出变更并经本人原生 SDK 审批保存、三模式固定指令版本及旧执行追加历史入口。规则工具只有对应不可变写回执存在才可显示已保存，避免普通错误输出误报成功；规则正文不进入共享工具摘要。三执行身份 Go+SDK 确定性联跑及独立 UI 已通过；完整 SDK 726、前端 319 通过的代码版本和之后 G4.17 的增量须分开，详见 goal-progress。手机首页挤成窄列的问题由截图发现并已修复。上述更新不代表原生 Sandbox、通用协作、附件追加、失败安全继续和外部模型全流程完成。

G4.17 未领取输入的撤回/修订已通过 Runtime 竞争/迁移/权限、三模式 Go+SDK 联合重建以及前端 323 项回归。已领取输入继续拒绝覆盖，旧正文及新修订分开保留，详见 goal-progress 的最终批次。

G4.18 已接三模式追加附件、原生图片/文本输入和选择/上传入口，并通过较早三模式 Go+SDK 联跑及独立 UI。review 发现并修复媒体下载未固定快照和字节校验、4 MiB 图片零模型暂停时重复存储导致检查点超限；又增加解析文本版本绑定，防止相同原文件快照下的重新解析改变模型输入。后续最终回归仍在进行，早期 800 项不能代替后续同版全量。详情及失败/权限边界见 goal-progress；本文件仍非原目标的最终 review，不能在此结束 Goal。

G4.19 已接三模式模型临时故障的原生 RunState 暂停/显式继续，Go 再检查未决工具及未知写入结果，保留原 attempt、轮次预算和已完成文件。修复输出 guardrail 后漏记收件、新事件白名单遗漏、故障后原生输入与回执账本不一致，以及首次恢复边界无进展递归。Go 专项、SDK 146 项、UI 68 项和三模式故障恢复/附件真实 Go+SDK 联合 140.237s 通过。模型均为替身；新浏览器截图启动审查两次超时，未通过。未决写入核对、原生持久工作区、通用协作、最终同版全量及最终全局 review 继续执行，详见 goal-progress 的 2026-09-08 核定。

### G4.44 跨模块授权复查（2026-09-09）

上方2026-09-07的缺口表保留当时语义；G4.20至G4.43的补齐和证据以`agent-platform-goal-progress.md`为准，不能再把已接的工作区/子任务等说成完全未开发。本次全局代码review继续进行，但并未完成全部边界，真实Go/OCI、网关和用户页面验收仍不通过。

- **[P1，修复源码、行为待验] Skill管理在预检查后撤权仍可能提交**。普通安装未经过草稿的事务授权分支，启停/切版本/卸载还先变更活动目录。现由`authorizeSkillScopeMutationQuery`在写事务内核对当前成员、角色及项目存续，所有路径在活动目录变更前授权；原草稿审批/精确快照检查和文件回滚保留。位置：`skill_scope.go:96`，`skill_installation.go:409`、`:865`、`:984`、`:1070`。
- **[P1，修复源码、行为待验] 项目文件接口在事务内信任旧认证角色**。共享`projectFilesWorkspace`现通过同一query读取当前权限和用户/工作区存续状态。降级viewer保持读权限，但不能写；撤权或禁用后读取、下载、写入均拒绝。位置：`project_files.go:113`。此助手被其他执行/规则/文件入口复用，后续Go全量须覆盖其调用方，build/vet不能代替行为回归。
- **[P1，修复源码、行为待验] 后台Worker令牌轮换后旧请求仍可落到活动任务**。后台工具事务此前只验证状态/租约，未像状态化分支一样复核请求中的attempt/token。现核对单一执行身份、项目、attempt及当前token；显式service不能省略activity。位置：`agent_task_checkpoint.go:286`。旧回执幂等/终态收尾没有被改成新的写入许可。

回归源码：`skill_mutation_authorization_test.go`、`project_files_test.go:178`及`:406`；所有Go新用例**未执行**，仍受已记录的Application Control限制。最后全后端build、六包vet退出0，日志`goal-g444-review-build.log`/`goal-g444-review-vet.log`。真实SDK三父入口的子任务暂停/取消矩阵和既有专项24通过；包含六个新增用例的全量1499通过，373.64秒，`goal-g444-sdk-all.xml`。这些是不同层的证据，不能拼接为Go+SDK真实联合验收。

本次已阅读内部服务/委托用户身份、Skill管理、项目文件/快照发布、MCP加密凭据/消费、规则事务/持久回执、外部工具结果成功门禁，并检查MCPConnections、ProjectFiles、AgentToolOutcomeReview源码。其他执行恢复、初始化/配置/迁移和完整前端操作链仍待继续；没有新的浏览器或真实环境证据。未部署或操作用户服务，未变更schema/系统策略；**不结束完整Goal，不签署最终review通过**。

### G4.45 生命周期及迁移审查（2026-09-09）

- **[P1，已修复并隔离回归] 流式断连后执行清理与登记释放不一致**。事件消费取消会遗留队列getter；响应头或事件发送失败时原StreamingResponse未主动关闭生成器，执行/工作区清理可能未结束就释放turn ID。`runtime.py`显式关闭并等待执行，队列任务在finally回收；`app.py`使用专用关闭响应，在清理完成后释放登记，AnyIO取消期间也等待原清理。五项原代码全部失败；修复后覆盖ASGI 2.0/2.4、头/事件断连、清理等待期间拒绝重复执行及之后重新进入。证据`goal-g445-stream-before.xml`、`goal-g445-stream-after.xml`。
- **[P1，已修复并隔离回归] 部分Worker启动失败漏清理、某Worker关闭失败导致其他清理未被等待**。lifespan的try/finally现覆盖构造/启动，先登记再启动；关闭收集所有结果后再报脱敏错误，不在第一项失败时提前返回。新增三项覆盖部分构造失败、再次启动、取消和失败期间仍等待所有stop；与stream/app/runtime合计29通过，8.83秒，`goal-g445-lifecycle.xml`。
- **[P1，修复源码、行为待验] 组件回填提前把整个schema标记为完成**。`store.go`将user_version更新从配置快照回填移至所有数据库回填之后，与migration_history成功回执在同一最终事务提交。报告失败不推进版本，不删除原回滚备份。`migration_test.go`新增两种失败后重试/保留备份及组件不推进版本用例，Go限制下未执行。不是把全部升级改为原子事务，文件报告也不代替数据库的提交状态。

本批已继续读取执行控制、主对话/后台暂停恢复、排队编辑、检查点和服务初始化配置。恢复调用方、完整前端链和跨模块收尾仍需覆盖，不声明全局review完成。完整SDK1507通过，259.55秒，0失败/错误/跳过，`goal-g445-sdk-all.xml`；Go仅build/vet退出0，`goal-g445-build.log`/`goal-g445-vet.log`，没有Go行为或真实环境新证据。前端未改，沿用G4.43证据。明确声明已安装AnyIO依赖，无安装或版本升级；未部署、操作用户数据库或服务、真实OCI、网关或系统策略，Goal保持active。

### G4.46 前端断线与状态隔离审查（2026-09-09）

- **[P1，已修复并组件回归] SSE断线只恢复状态化Run，其他SDK执行状态停滞**。`App.tsx`对活动主对话/后台/状态化执行统一刷新完整projection，含等待审批；限制轮询重叠，重连/终态停止，旧响应不可覆盖更新。六种执行/状态组合及慢请求重连回归通过。
- **[P1，已修复并组件回归] 刷新异常卸载整个工作台，造成未提交草稿消失**。瞬时失败保留已加载界面并显示错误，401/403/404则撤下操作入口。覆盖草稿、恢复及撤权三类状态。
- **[P1，已修复并组件回归] 作品及产物切换混用旧状态**。Workbench按project ID隔离，ArtifactWorkspace按artifact ID隔离；旧版本在新正文读取期间不再显示/编辑或进入Composer上下文，前一产物迟到历史响应不再弹到新产物。原代码跨作品失败消息和旧正文用例均失败，修复后通过。
- **[P2，已修复并组件回归] 规则保存未知结果后重试换request_id，无法取回原持久回执**。`AgentInstructions.tsx`按规范化命令保留请求身份；同内容/清空重试复用，修改命令换ID，CAS不放宽。模拟已提交但丢响应的两个分支通过。

前端全量367通过，0失败/待定，`goal-g446-ui-all.json`；TypeScript、隔离Vite构建通过。初次19项中12失败的复现日志和修复后70项专项均保留，见goal-progress。没有真实浏览器、Go或模型E2E新证据，后端/SDK源码本批未动，不能将这些组件结果拼成最终平台验收。

**剩余明确发现**：普通消息发送失败清空输入/附件、Enter绕过上传等待/输入法合成边界，以及产物历史读取异常无可重试反馈仍需修复。后台恢复领取首条queued checkpoint出错后保持队首，需补损坏状态隔离与后续任务不饥饿；主对话checkpoint调用链已查到内部manager显式绑定generation，没有将助手兼容零值误记为公开接口漏洞。完整review继续，不签署通过、不完成Goal，不部署或修改用户环境。

### G4.47 发送恢复与后台恢复队列审查（2026-09-09）

本批处理上方G4.46的明确待办；该段保留历史语义，以下为当前证据。

- **[P1，已修复并组件回归] 普通消息失败丢输入/材料，同一请求重试可能新建一轮**。`App.tsx`保留未确认草稿、Skill和附件，用规范化内容、引用版本及上下文保存请求身份；相同重试复用，未知结果后修改需确认。首页handoff沿用原键。Workbench按请求身份复用乐观消息，旧请求不能覆盖后来消息的缓存。停止后迟到响应不能清除新草稿。
- **[P2，已修复并组件回归] Enter绕过上传等待或提交输入法候选字**。提交函数检查uploading/活动请求，键盘路径识别isComposing和229；用真实组件事件与受控上传验证等待及成功后的文件绑定。
- **[P2，已修复并组件回归] 历史读取失败无用户反馈**。历史加载有等待状态、错误和重试，失败保留当前正文，沿用按artifact ID隔离的组件生命周期。
- **[P1，修复源码、行为待验] 损坏后台checkpoint永久占据领取队首，缺失状态还可能被当作新执行**。`agent_task_checkpoint.go`校验checkpoint schema/hash/version、所属attempt及完整审批绑定；暂停attempt缺少状态拒绝fresh执行。`agent_task.go`仅对明确损坏在事务内隔离该任务，保留审计/checkpoint，下一轮领取健康任务；普通数据库错误仍回滚。`execution_control.go`保留损坏attempt重放屏障，即使checkpoint缺失也禁止手动重启。新增十项Go场景包含审计失败回滚，**没有执行**。

前端新增九项；八项初始失败复现记录及全部修复专项保留。首次全量374通过、两项旧调用参数断言失败，补新增请求标识断言后最终376通过，0失败/待定，`.tmp/goal-g447-ui-all-final.json`。类型检查和隔离Vite构建退出0，`.tmp/goal-g447-ui-types-final.log`、`.tmp/goal-g447-ui-build.log`。SDK本轮全量1507通过、277.11秒，0失败/错误/跳过，`.tmp/goal-g447-sdk-all.xml`。Go仅build/vet退出0，`.tmp/goal-g447-build.log`、`.tmp/goal-g447-vet.log`；这些不能拼成真实Go/SDK/用户页面联合证据。

所有本批测试已结束。原功能清单的跨模块覆盖收尾、Go行为及真实环境验收仍未闭合，不将本批修复称为最终review通过；仍不部署、不操作用户环境、不修改系统限制，完整Goal保持active。

### G4.48 提交关联与接受权限审查（2026-09-09）

- **[P1，已修复并组件回归；Go派生字段行为待验] 使用正文/时间/asset ID猜测提交身份**。独立的重复请求可被合并，迟到或排队改文案又不能正确结算。`agentTurns.ts`改用确切`submission_id`；`optimisticMessage.ts`普通合并只认message ID。Go在接受及读取时派生绑定用户、会话和幂等键的SHA-256，不暴露原始键、不增加执行权限或数据库迁移。已提交请求只有对应持久正文到达才撤下本地行；旧服务/缺WebCrypto时不再猜测，等待直接回执。
- **[P1，已修复并组件回归] 停止HTTP等待被误报为SDK已停止，同键重试还会被旧传输覆盖**。`App.tsx`区分提交和单次传输身份，未知结果保留正文/附件、显示发送结果未确认并刷新权威projection；只有服务端取消才展示已停止。旧失败不能修改新传输状态，确切成功/投影清理原待确认缓存。没有为完成状态而重发模型请求。
- **[P1，修复源码、行为待验] 接受消息仅检查工作区，未在事务内复查角色/存续；幂等回执未绑定作者**。`agent_turn.go`在写入和缓存回读前复用当前执行者权限检查，回读查询绑定用户、工作区、项目及会话。`agent_turn_acceptance_auth_test.go`新增撤权、降权、禁用、同键跨作者拒绝及合法独立请求源码，未运行受限制的Go测试。

七项新增UI反例最初均失败；修复扩大专项64通过，最终前端386通过、0失败/待定，`.tmp/goal-g448-ui-all.json`。中间两项失败来自共用夹具项目与持久本地草稿，改独立项目后通过，失败记录保留。真实WebCrypto配合HTTP/projection夹具验证回执丢失恢复，不是实际Go或浏览器E2E。最后类型检查、隔离构建、Go build/六包vet退出0；SDK未改未重跑，最近完整证据仍为G4.47的1507通过。

本轮重新核对原完成态、SDK目录、用户任务及发布脚本，未发现内置Skill ID或绕过SDK的直接模型调用，但该扫描不证明完整功能。下一步继续原发布/回滚/验收脚本和四条原业务流程证据覆盖检查；尚未运行的Go/真实环境门禁保留。语音/实时、Computer、PTC的原待明确范围已异步询问，不静默排除或提前声称已接入。完整Goal保持active，未部署或改变用户环境。

### G4.49/G4.50 发布假通过与业务回滚审查（2026-09-09）

- **[P1，已修复并夹具回归] 发布报告可因矩阵缺项/降级及外部配置passed而错误ready，失败还可能沿用旧成功**。当前报告在预检查前写running，逐项保存状态，拒绝缺少/重复/降级的必需门禁；外部声明保持unverified。记录运行前后源码和矩阵哈希、独立输出，本地通过不签署发布或最终review。前端验收构建不再覆盖现有dist，烟测绑定本次成功产物。
- **[P1，已补校验接线、真实Go行为待验] 空库启动及备份复制hash不足以证明业务恢复，按时间选备份也未绑定迁移**。`run-agent-platform-rollback-drill.ps1`新增业务夹具前后读取、精确migration_history选择及新目录恢复；`agent_platform_rollback_data.py`验证完整SQLite逻辑摘要、字段/数量和独立文件字节，含历史版本和重复消息。恢复使用SQLite backup而不要求VACUUM前后物理页hash相同，不复用迁移后原始文件目录。
- **[P1，修复源码、真实进程行为待验] 回滚清理尚未完成即写passed，首个失败会打断其他清理且遗漏恢复Go环境变量**。现在确认退出、逐项尝试所有清理及环境恢复，最后才写本项结果；路径检查拒绝越界/reparse，默认拒绝启动8860/8880。预检查失败路径有实际脚本副本验证，真实Go进程清理未验证。

业务辅助函数、真实SQLite文件/backup、真实CLI及随机loopback假HTTP服务共28项通过（64.139秒，`goal-g450-rollback-data-verified.log`）；DDL来自基线SQL，迁移回执明确为夹具，不能当Runtime迁移/旧版API通过。最终报告/语法/缺工具链入口测试51项通过（`goal-g450-release-evidence-final.json`）。真实Go/OCI/网关和用户页面仍未验收，完整资源/配置/部署回滚仍需覆盖；原四流程与用户任务联合覆盖继续核对，不签署最终review、不完成Goal，不部署。

### G4.51 配置卡与提案事务审查（2026-09-09）

- **[P1，已修复并组件回归] 用户看到的配置与确认启动的配置不同，迟到保存还能覆盖新版提案**。`RunConfigurationCard`原表单状态只在首次挂载初始化；现在按提案版本/快照隔离表单，同版保留草稿，新版显示权威配置，卸载表单的旧保存不更新父级；父直接回执只推进pending且较低版本。完整projection与本地更新竞争仍待后续覆盖，不能扩大此结论。
- **[P2，已修复并组件/API回归] 保存未知结果后手动重试换幂等键，失效动作仍可编辑，提案事件不刷新页面**。同规范化配置重试复用原键；保存期间锁定字段；非pending/consumed提案只读；created/configured/input_bound/consumed事件接入既有防抖刷新。
- **[P1，源码已修复、行为待验] 配置事务丢失冻结Skill绑定，输入/配置写入及缓存读取未复查当前权限**。`ConfigureProposedAction`在使用前设置原提案ID；两个变更入口在事务中核对项目、当前成员/用户/工作区、当前及原editor权限，再访问幂等记录。缓存回执绑定确切项目/提案，拒绝跨scope。新增两操作各六类授权场景及同公开版本不同Schema的冻结选择用例，Go测试未执行，仅build/vet通过。

前端最终399通过，0失败/待定；专项96通过，TypeScript及隔离Vite构建退出0。第一轮12项失败及首次修复后的4项测试写法问题均保留证据，见goal-progress G4.51。SDK未改未重跑，不以旧1507项或本轮静态检查冒充真实Go/SDK/页面验收。

明确未关闭：配置Schema读取仅按公开版本而非提案冻结快照；三启动分支的事务当前权限与回执归属；前端启动和旧消息自动配置的手动重试；先请求projection后收到配置回执时的旧投影覆盖。继续同一原业务确认链及完整任务覆盖，不签署最终review，不部署、不改变用户环境，Goal保持active。

### G4.52 确认链剩余问题及原流程验收覆盖（2026-09-09）

上方G4.51明确待办由本批源码和分层证据更新，不再称这些逻辑尚未开发，但Go行为回归仍未通过。

- **[P1，源码已修复、Go行为待验] 相同公开版本不等于相同配置包**。配置GET传递提案ID，Runtime在只读事务内核对身份/当前成员和确切项目、提案、Capability、版本，使用invocation冻结包生成Schema。新路由绑定、同版本影子包、跨项目/版本拒绝及只读权限测试源码均未执行。
- **[P1，源码已修复、Go行为待验] 启动可读取撤权前的回执，缓存也未核对原消费目标**。传统/managed Run和background Task三个入口在幂等回读前复查当前权限及Skill可用性；成功回执需匹配该提案实际消费的Run/Task及项目/会话。新增三模式×首次/回执×四权限场景及消费目标反例，只有build/vet证据。
- **[P1/P2，已组件/API回归] Schema未就绪仍可提交，默认配置来自活动版本；启动重试换键，旧投影覆盖新回执**。读取失败、版本不符或Skill不可用时禁用并提供重试，草稿和显式选择保留，默认项取冻结包；消息后续绑定/配置和启动按命令复用键，确认快照变化才换键；直接回执使先开始的旧投影失效。完整App夹具证明重复绑定不增加用户消息，但不是用户服务E2E。

最终前端413通过，专项110通过；类型检查、隔离构建、全后端build和六包vet退出0，详见goal-progress G4.52。SDK未改未重跑。此前失败日志保留，加载门禁引起的旧测试时序调整不计成额外产品缺陷。

**[P2，尚未修复] Capability合同门禁漏掉原第四条流程**：`acceptance/validate-capability-contracts.mjs`固定读取三个manifest，没有`script-continuation.json`；实际运行虽退出0，仅报告3个manifest/34步。旧Stage8计划的99/99分配校验及four-mode烟测也不能作为当前四条业务链的同版验收。下一步补齐这项原范围覆盖，并核对完整W0至W9/GAP/用户任务门禁。真实Go、网关、OCI、用户页面及完整回滚未验收，仍不签署最终review，不完成Goal，不部署。

### G4.53 第四流程门禁遗漏，已修复并隔离回归（2026-09-09）

上方G4.52的P2为历史发现，合同校验遗漏已在本批修复，不再列为待修改代码；真实四流程验收仍未通过。

- 旧校验器的32项正反例中3通过/29失败，证实删掉续写流程、取消单选审批或删掉提示词仍会假通过。首次Node `spawn EPERM`属于测试启动失败，经权限复核后才取得上述反例，不混算。
- 四manifest/37步、文件身份与流程顺序、续写确认版本及决策快照、单选审批/新版本失效、字数配置、五方向Schema和结果适配器已纳入静态检查。修复后32项全部通过，实际仓库合同校验通过，发布动作接入反例自测。
- 发布报告51项夹具通过；只证明报告逻辑和缺工具链副本的预检查行为，不运行真实Go/浏览器/发布动作。旧Stage8的99/99结果仅为计划分配证据。
- 现有续写Go测试已经覆盖原稿确认、五选一、冻结选择、正文审批完成及完工后修订，但当前源码版本未执行。还需核对原四流程完整用户操作和交付证据，不能用静态合同或旧烟测代替同版运行验收。详见goal-progress G4.53；外部条件及不部署边界不变，最终review结论仍未通过。

### G4.54 共享审批交互与恢复归属（2026-09-09）

- **[P1，前端已修复] 审批版本变化仍显示旧候选，确认后可能恢复另一Run**：卡片现在绑定完整审批身份，旧状态/异步结果不跨版本；确认后仅查询及恢复approval.run_id，核验快照和动作目标，不猜测项目当前运行。未知响应复用请求键并刷新projection，版本/正文改变才换键。
- **[P2，前端已修复] 内容读取失败无法恢复、通用单选固定五项及提交状态不完整**：新增读取错误重试及版本/候选格式校验，通用单选支持不同候选数，续写五方向仍由合同/Runtime约束。提交期间锁定字段，函数防重复，viewer/非pending/不允许动作及竞争返修均不能提交。其他改编、扩写、逐集和质量审核路径已有组件兼容测试。
- 初始组件16项2通过/14失败，App恢复30项26通过/4失败；修复和扩展后专项152项通过，最终全量457项通过、0失败/待定，类型检查与隔离构建退出0。具体证据与一项Error夹具文字修正在goal-progress G4.54。所有结果为组件、完整App的API夹具及请求传输，不是真实Go或浏览器E2E。
- **[P2，未修复] 续写结果没有文件交付入口**：前端ArtifactWorkspace及后端artifact_delivery只支持两类generic产物；续写没有创建可供顶部导出的聚合候选。下一步补通用版本交付，不能宣称四流程端到端已通过。
- **[待核定] 审批/返修后端当前授权与重试边界**：`ResolveApproval`、`RequestApprovalRegeneration`及HTTP实际调用的`RequestApprovalRevision`需继续核对事务内权限与缓存归属；后者没有显式校验审批动作白名单。另需核对质量审核手动编辑的目标Run。前端隐藏按钮不是后端授权证据；本批未修改或执行这些Go路径。
- SDK及Go源码本批未变，未重跑其全量；未修改用户运行环境或部署。Goal仍active，最终全局review未通过。
