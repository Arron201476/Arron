# 阶段 6 前端实现验收

状态：已完成；阶段 7 的 B7-0 补充交互验收已通过  
日期：2026-08-05  
运行入口：`http://127.0.0.1:8860`

## 1. 验收结论

独立桌面 Web 前端已在 `frontend/` 完成，不运行或引用旧项目的前端代码。实现以阶段 4 最终视觉基线、阶段 5 API/Event/Runtime 合同为输入；业务状态只读取 Project、Run Snapshot、Approval、Artifact、Event 和 `available_actions`，不根据 Agent 文案猜测状态。

阶段 6 的基础工程、自测和实图检查已完成。2026-08-05 的阶段 1-6 代码复盘发现质量审核返修和普通审批非批准动作存在声明/实现漂移，详见 `stage-1-6-code-review.md`。本文件原“实现完成”结论由该复盘结论覆盖。

## 2. 工作包结果

| 工作包 | 状态 | 实现证据 |
|---|---|---|
| F6-0 工程基线 | 完成 | React 19、TypeScript、Vite、Lucide、Vitest、Playwright；独立构建和代理配置 |
| F6-1 作品入口 | 完成 | 新建、打开、重命名、搜索、删除影响预览、二次删除确认、锚定操作菜单 |
| F6-2 通用工作台 | 完成 | 作品顶栏、Artifact 目录、中央编辑区、Agent Timeline、固定底部 Composer |
| F6-3 Agent 与能力入口 | 完成 | 普通聊天、显式能力菜单、结构化 `capability_ref`、配置卡、启动二次确认 |
| F6-4 Timeline 与审批 | 阻断 | approve、最终稿确认和风险接受已接通；AI 返修、手动修改确认和重生成尚未形成专用命令闭环 |
| F6-5 产物与版本 | 完成 | Artifact Registry、正文渲染、手动编辑、新版本、版本历史、未保存离页保护 |
| F6-6 视频批次 | 完成 | 分批上传、自然顺序、原子人工换序、缺集显式确认、封存、逐集状态、失败项重试入口 |
| F6-7 候选与最终稿 | 完成 | 候选选择和中央预览、最终稿确认请求、TXT/DOCX 导出 |
| F6-8 恢复与质量 | 完成 | Project SSE、断线状态、事件回补读取、冲突错误呈现、键盘焦点闭环、响应式与长列表 QA |

## 3. 自动验证

### 3.1 生产构建

```text
pnpm build
TypeScript 检查通过
Vite production build 通过
```

### 3.2 协议单元测试

```text
pnpm test
1 test file passed
7 tests passed
```

覆盖：

- 显式能力必须发送结构化 `capability_ref`；
- 视频相邻集换序必须在一个 Asset Set Version Command 中原子提交；
- DOCX 导出格式和目标 Artifact Version 不得丢失。
- 空 MIME 文件按扩展名正确分类，混合上传不污染视频批次；
- 最终稿确认和质量风险接受使用各自专用 Command；
- 不确定网络失败使用同一个 Idempotency-Key 重试。

### 3.3 浏览器自动化

脚本：`frontend/scripts/visual-qa.mjs`

通过场景：

- 作品菜单为锚定菜单，不误开模态框；
- 删除必须先展示影响，Esc 可关闭，焦点不能逃出模态；
- 显式选择小说能力、发送、配置集数/时长、二次确认、创建 Run；
- 视频上传、建立批次、识别集号、封存、解析配置、二次确认、创建 Run；
- 运行中展示逐集成功、处理中和失败状态；
- 运行暂停/恢复/失败重试由 `available_actions` 投影，结束运行有危险操作确认；
- 候选稿、最终稿和 TXT/DOCX 导出菜单；
- 50 项目录压力视图；
- 1280、1440、1920 三档桌面宽度无横向溢出。

## 4. 响应式实测

| 视口 | 右侧 Agent 宽度 | Composer 高度 | Composer 底部空白 | 发送按钮 |
|---|---:|---:|---:|---:|
| 1280×800 | 348 px | 160 px | 0 px | 32×32 px |
| 1440×900 | 392 px | 160 px | 0 px | 32×32 px |
| 1920×1080 | 420 px | 160 px | 0 px | 32×32 px |

实图和机器报告位于 `artifacts/stage-6-frontend/`。其中包含作品入口、删除影响、能力菜单、运行启动、视频逐集状态、导出菜单和 50 项压力视图。

## 5. 阶段 7 联调门禁

以下不是前端用本地状态绕过的问题，必须由阶段 7 的 B7-0/B7-2 收口：

1. 后端尚未提供“当前未消费 Proposed Action”查询；刷新后配置卡恢复不能由浏览器副本冒充 Runtime 真源。
2. 合同定义了 `GET /api/v1/projects/{project_id}/snapshot`，当前后端未实现；前端暂由多个权威 Query 组合恢复，并在 SSE 事件或 `stream.reset_required` 后重新读取。
3. Windows Application Control 阻止新编译 Go Server EXE 启动，因此本轮浏览器流程采用严格匹配正式 API Envelope 的 Route Mock；真实前后端联调归阶段 7 支持环境。
4. 豆包视频网关 HTTP 504、正式 Provider 凭据、Worker 部署与 40 集真实样本不属于阶段 6 完成声明。

## 6. 明确未宣称

- 未宣称新项目真实后端已在当前 Windows 策略下启动；
- 未宣称豆包 Provider 问题已关闭；
- 未宣称 40 集真实视频 E2E 已通过；
- 未进入移动端、登录权限、新 Capability 或发布部署范围；
- 未将 Mock 浏览器测试描述为阶段 9 的真实样本验收。

## 7. 阶段门禁

`stage-1-6-code-review.md` 中两个 P0 已关闭。由于本地前端未连接可运行的真实后端，产品负责人无法验证核心交互，因此不再以不可操作页面阻塞后端联调：

```text
阶段 6 前端范围、交互和当前视觉作为阶段 7 真实 API 联调基线；B7-0 接通后立即补做产品交互验收。
```

2026-08-05 有条件进入阶段 7；2026-08-06 产品负责人完成真实后端页面检查，确认作品 CRUD、普通聊天、三个 Skill 入口、小说配置卡和刷新恢复无明显问题，B7-0 与阶段 6 补充验收正式关闭。
