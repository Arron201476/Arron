# 改造后能力补齐与验收

日期：2026-09-06。基线：`6f964d1`。仅本机检查、开发和验证；未经授权不 push、不部署、不连接测试机。

2026-09-06 最新边界：用户要求先记录问题，并质疑 SDK 核心能力是否还有遗漏。开发保持暂停，新增 `agent-platform-capability-rebaseline.md` 登记对话创建 Skill、文件交付、错误操作指引、压缩阻塞及完整性基线问题。下文是已有局部实现与验收证据，不证明全量能力已覆盖，也不是已完成的全局 review。

2026-09-06 继续核验：[核心能力覆盖审计](/C:/Users/egois/Desktop/内容生产Agent项目/docs/agent-platform-capability-audit-20260906.md) 已列出更多 SDK/Host/前端接入缺口，并以离线 SDK 校验复现 Hosted 延迟加载契约错误。当前仍处于能力覆盖核验阶段，没有恢复应用代码修改，也没有开始独立全局 review。

## 先核实前后端边界

此前“本地代码完成”的结论不能解释为“全部承诺的能力已可用”。本次以当前代码、运行中的本地 API 和实际页面为证据，不沿用历史测试数量作为完成证明。

## 交付基线，不缩减范围

用户于 2026-09-05 明确：当时承诺的功能必须具备，SDK 核心能力应能够在平台中实际使用。补齐这些缺口是原任务，不是可选扩功能。

- 原 `agent-platform-full-convergence-plan.md` 的完成态、W0-W9 和全量验收矩阵继续有效；历史 completed 标签不是豁免后续发现的缺口。
- SDK 核心能力按 Agent/Runner、工具调用、多 Agent 协作、Session/原生压缩、流式事件、审批与恢复、结构化输出、guardrails、tracing/用量逐项核对。不得根据现在已经接了哪些 API 反过来缩小清单。
- 对每项分别记录后端执行、前端入口或自动触发路径、实际回归证据。底层自动运行的能力不强行添加按钮，但必须可以验证其确实运行。
- 三种执行模式分别检查；主对话可用不能证明后台任务或状态化工作流也可用。
- 缺配置、账号权限或运行环境要保留为未完成的验收项。禁止绕过安全策略或擅自部署测试机；说明阻塞不等于取消承诺。
- 拒绝不支持的配置只能算防故障修复，不等于实现该能力。例如 Hosted Tool 的契约收紧不能算补齐 Hosted Tools。
- 用户最新要求的执行顺序：先补齐全部承诺并验收达标，再启动独立的全局 review。目前属于第一阶段，只进行实现必需的自查与回归；不得将这些检查宣称为全局 review 完成。

## 能力现状

| 能力 | 后端现状 | 前端现状 / 验证边界 |
| --- | --- | --- |
| SDK 主对话 | 实际用户请求有 Responses ID、用量和真实工具记录；主循环使用 SDK Runner | 已补执行记录组件，展示模型、用量、失败阶段及成功工具调用；已做本地页面验证 |
| Skill 动态发现 | 本地 SKILL.md 与管理式 ZIP 均进入注册表；标准无显式版本的 Skill 使用内容哈希版本；按归属过滤后选择 user > project > workspace > system；v37 为目录和管理包保存不可变执行绑定 | v36 已接个人/项目/工作区安装及纳入管理，平台范围只读；v37 主对话接收时固定完整 Skill 目录，后台沿用发起对话的目录，目录修改不影响原任务。已补检查已注册目录更新、预览来源与哈希、显式更新入口；不是文件监听或自动同步 |
| Skill 按需加载 | 已补固定版本资源目录、UTF-8 文本分页读取、长指令后续读取及项目权限检查；支持 PNG/JPEG/GIF/WebP 和 PDF/DOCX 通过 SDK 原生 ToolOutputImage/ToolOutputFileContent 按需送入模型 | 原生 SDK 回归覆盖图片/文件输入、大小限制和审计中不保存 base64；10 MiB 媒体经过审批 checkpoint 与恢复通过，Go 私有 SSE 和数据库 14 MiB 状态回归通过。真实模型对各媒体格式的支持仍待验收 |
| 后台 Skill | 已补项目/Skill 工具、MCP/Hosted 审批、SDK RunState 持久化、同一 attempt 恢复、Token 轮换、租约及取消检查；后台结果使用 SDK 非严格 Schema 声明并保留 Pydantic 校验 | 已补等待工具授权状态、取消及危险重放提示；SDK 回归覆盖审批前不执行、重建 Worker 后批准/拒绝，Go 覆盖重启、四次审批不消耗重试预算、取消/删除及过期租约拒绝写入；真实前端服务端联合验收仍待完成 |
| 持久工作流 | manifest + workflow.json + schema 可注册；Run/Task/审批由 Runtime 持久化 | 通用 schema 配置已存在；需要复核材料要求、版本切换和失败路径 |
| MCP | 有 stdio/HTTP/SSE、白名单及主对话审批/审计适配；v38 增加工作区可见性、开关和调用配置快照；本地 stdio fixture 已通过 | 已补管理员配置开关、依赖刷新和并发版本检查；隔离环境真实模型已完成 MCP 读取，写审批链路发现问题后正在复验。用户凭据实际消费及真实外部集成仍未闭环 |
| Hosted tools | 第一阶段新增四类原生 SDK 工具适配，通过 Agent.as_tool 接入统一审批、审计、取消和结果上限；生成文件复用项目 Asset 存储 | v38 增加管理员配置开关；文件搜索未配置受信任向量库时不能开启。真实网关和完整产物下载仍待验收，不能仅以适配测试通过宣称达标 |
| Session / compaction | SDK SQLiteSession 和原生 Responses compaction 路径存在；已增加替换前协议校验，拒绝普通摘要覆盖原会话 | 32 轮真实网关试验：工具调用全部完成，11 次 compact HTTP 200，但输出不是原生 compaction 项，重启回忆失败。保护复测拒绝普通 user 摘要并保留 13 条原始项。当前网关不达标，已向用户询问修复或替换接口；不使用本地摘要回退 |
| Skill 脚本 | OCI 沙箱、审批及审计实现存在；v37 普通目录可直接绑定不可变脚本副本，无须伪造管理式安装；标准 Skill 自动发现 Python 入口，显式 scripts 清单仍为准 | 管理页有开关；目录脚本审批、修改后重启仍执行原副本、禁用包/策略时中止的 fixture 回归通过。本机真实 OCI 运行条件未满足，不能算真实执行验收通过 |
| 多工作区权限 | 身份、资源归属、配额、审计与 ZIP 隔离；v36 接入真实用户/项目/工作区目录选择、独立活动目录、私有安装记录和失败诊断过滤 | HTTP 覆盖同项目不同用户资源选择、跨用户拒绝读取/管理、跨工作区项目安装拒绝；编辑者可管理个人/项目包，公共包需管理员。共享项目中不能代他人启动或重试个人 Skill |
| 多 Agent 协作 / guardrails | 已加入由本轮候选 inline Skill 动态构建的原生 handoff，以及 SDK 输入、输出和函数工具 guardrails；保护提交前的数据边界，实际提交成功才结束 SDK 循环 | SDK 回归已覆盖延迟加载、固定版本、同一上下文 handoff、输入阻断、输出密钥保护和被拒提交后的修正；补充子 Agent 审批后重建完整图的批准/拒绝恢复，不退回根 Agent、不重放写请求。真实运行仍待验收 |

标准 Skill 的自然语言步骤由 Agent 阅读执行，并不自动等价于持久化工作流图。后者当前要求显式 workflow.json。这是产品能力边界，需要向用户说明，不应包装成已经自动建图。

## 已修代码与回归状态

以下表示本地代码已修改，不等同于所有能力已交付或当前后台进程已加载新代码。

1. 消息顺序：相同时间戳按服务端顺序稳定展示，移除随机 ID 排序；回归测试及实际页面验证通过。
2. Skill 可见性：管理页合并安装记录与目录发现项，显示来源、位置、依赖及状态；补回工作台管理入口。
3. 布局：移除 1180px 页面最小宽度，1135px 以下切换单面板；工作台 390px、820px 和当前桌面宽度无横向溢出。Skill 管理页本批已复查桌面、390px 和 820px；修正手机返回链接换行，长路径与控件无溢出。
4. Skill 资源：新增资源快照、内容哈希、路径限制、固定版本与 Unicode 分页；含跨工作区访问反例。长文本材料也增加分页，避免只读到前 48000 字符。
5. 后台任务：增加通用工具与原生审批恢复；SDK fallback 仅允许在尚未调用工具时重启，避免重放外部写入。工具与具体 attempt 绑定，审批恢复不新增失败尝试；取消、项目/工作区删除清理待审批与私有 checkpoint。
6. 工具契约与审计：四类 Hosted Tool 使用 SDK 原生工具并纳入统一审批/审计；修正 SDK 将工具异常转为错误文本后被错误记为成功的问题。媒体资源的审计记录仅保存摘要、大小和哈希。
7. 配置表单：续写目标字数可配置；无必需材料的通用 workflow 不再强制选材料，保留已有 user_notes。
8. 状态交互：不可用 Skill 不再可选；待处理动作不因后续消息被隐藏；增加 Agent 执行详情及工具配置清单。
9. Session：修正独立 :memory: Session 共用 bootstrap 标记、导致新 Session 丢失历史初始化的问题。
10. 本机访问保护：local_loopback 的修改请求及退出登录也校验同源；增加恶意 Origin 和同源反例测试。
11. SDK 协作与保护：动态 Skill handoff、输入/输出/函数工具 guardrails 接入；真正完成 commit 才停止主循环，拒绝的提交可以继续修正。
12. 数据库升级至 v34：增加后台 RunState、工具 attempt 绑定和脚本版本快照表；v32/v33 升级回归验证数据保留和迁移前备份。21:36 完成本机停服备份后已激活 v34，本机原有运行/对话状态数量不变；不连接测试机。
13. 脚本执行：在发起审批前绑定具体安装版本，SDK 传递选定版本及哈希，后台同时核对 Invocation；执行期间监控工具取消、任务租约和项目删除。回归覆盖审批期间升级并重启后仍执行原版、拒绝改版重放和禁用版本。旧审批无版本快照时要求重新确认，真实 OCI 引擎仍未满足。
14. 标准 Skill 版本：没有平台版本声明的非持久工作流 Skill 使用内容哈希版本；原有 `0.0.0` 安装记录只有哈希匹配才允许兼容，不修改历史包。回归覆盖升级、回滚与重启。
15. 目录管理：管理员可重新扫描已注册目录，并按所选版本和哈希复制到不可变安装包；不接收客户端主机路径，拒绝扫描后内容变动。卸载记录阻止目录自动回退，显式重新安装可恢复；HTTP 权限、工作区隔离及前端操作回归通过。
16. 本地代理：真实页面发现 Vite 字符串代理默认改写 Host，使同源写请求被 CSRF 拦截。改为显式保留 Host；浏览器扫描成功，真实 HTTP 验证同源 200、恶意 Origin 403，不放宽后端防护。
17. 媒体审批：SDK 原生 checkpoint 上限统一为 64 MiB，私有传输预留 1 MiB envelope；不采用本地摘要或丢弃媒体来恢复。支持的 10 MiB 单资源通过原生 SDK 审批恢复测试，超过整体 checkpoint 边界仍拒绝。
18. 执行身份（v35）：Invocation / Run 持久保存创建用户；SDK 对话和后台 Worker 使用独立 ContextVar 携带执行引用，不接受客户端自报用户。服务端从 Turn 或带 Token 的后台 attempt 恢复用户并重新检查成员权限，保留服务身份用于传输鉴权。过期执行不能读数据或发起新工具；工具收尾只允许访问绑定的原调用。身份、并发隔离、租约、伪造、跨项目和提交后收尾回归通过。该项是四作用域的前置工作，不代表四作用域已完成。
19. 不可变执行绑定（v35）：Invocation 和 Run 增加私有 skill_version_id，旧工作区安装版本在迁移时绑定一次，防止后来新安装的包反向认领旧执行。配置卡、启动、Context Pack、后续推进、重试与后台资源读取沿用原安装包。回归加入同名同版本高优先级覆盖，原任务仍读取原指令和资源。目录包历史归档和主对话选择快照在下方 v37 项中补齐。
20. 原生压缩协议保护：当前固定 SDK 的替换钩子在任何 clear/add 前校验有效 compaction 项；失败保留历史，不使用普通摘要、recent-N 截断或伪造加密项。补齐初始化失败时 Session 关闭及明确的 session_compaction 错误展示。真实失败报告保留于 `.tmp/session-acceptance-20260905-v35.json`，保护复测为 `.tmp/session-acceptance-20260905-native-guard.json`；均仅合成数据，不是业务项目 E2E。
21. 作用域选择（v36）：注册表先按真实归属过滤目录，再合并不可变快照和排序；已接全局/项目投影、能力定义、资源读取及执行选择。回归覆盖个人 > 项目 > 工作区、跨用户/工作区隔离、禁用卸载不回退、无绑定项目目录不全局生效。损坏的可见包显示不可用，不拖垮整个能力目录，也不降级执行低优先级包。
22. 作用域管理（v36）：安装范围由后端从当前用户或已验证项目解析；升级沿用原范围。个人/项目包分别写入 `data/skills/scoped-active/<workspace>/<scope>/<owner>/`，公共包保留原目录。安装记录和失败诊断过滤个人归属；同名跨范围安装不能绕过工作区配额。项目删除停止对应安装并清理活动副本，不删共享历史归档；重启不重新激活已删项目包。
23. 作用域执行绑定（v36）：Invocation/Run 的私有版本 ID 支持个人和项目包；脚本审批和实际执行都使用确切版本 ID，不再从同名同版本记录中任取一条。回归覆盖不同作用域同名版本、审批期间升级、重启、旧版执行、禁用不回退；个人后台待确认动作拒绝其他用户启动，手动重试/继续执行也检查个人归属。真实 OCI 验收仍阻塞。
24. 前端作用域入口：管理页提供个人/项目/工作区安装选择及真实项目菜单；项目未选择时不能安装。后端能力接口决定可开放的范围，旧后端只保留公共安装，不误接个人/项目请求；编辑者不能修改公共安装。安装诊断按范围关联，详情不展示另一个同名包的文件位置。11 条组件回归及本地桌面、390px、820px 页面检查通过，无横向溢出；本批未向业务数据库上传新的测试包。
25. 兼容性探针：与运行时使用同一原生 compaction 项校验，外层 `response.compaction` 标记不再足以判为支持；普通 user 摘要反例被拒绝，避免验收假通过。
26. 执行目录快照（v37）：接收主对话时保存完整 Skill 选择，包括空目录和不可用占位；中途安装、升级或出现高优先级包不改变本次选择。Invocation、Run 和脚本审批固定同一副本，后台沿用发起对话的能力目录；服务重启不切回活动版本。禁用/卸载仍阻断原包，不回退到同名其他包。
27. 普通目录归档（v37）：原目录不移动、不修改、不自动创建管理式安装；执行副本保存在 `data/skills/execution/<workspace>/<snapshot>/`。回归覆盖同版本内容修改、原目录删除、重启、后台领取与资源逐字一致。未选用包归档失败不阻断普通聊天；启动时清理事务回滚留下的无引用副本，保留已提交副本；工作区删除清理对应执行目录。
28. 普通目录脚本（v37）：脚本审批支持管理版本或目录执行副本，校验所选版本和哈希；标准 `SKILL.md + scripts/*.py` 不必额外编写平台 manifest。自动入口 ID 由相对路径生成，重名子目录不冲突；显式 scripts 清单（含空列表）覆盖自动发现，不扩大 Python 沙箱的运行边界。旧 v36 审批和执行历史迁移保留，旧未绑定审批仍拒绝执行。
29. 运行中权限撤销（v37）：脚本运行监控除了工具取消、项目删除和后台租约，还检查原包禁用与工作区脚本策略关闭；三个撤销路径均通过阻塞式沙箱 fixture 回归。真实 Docker/Podman 仍未获安装授权，未声称通过 OCI 实跑。
30. 已管理 Skill 的目录更新：GET 预览当前可见的同名已注册目录，展示来源范围、路径、版本和 SHA256；POST 必须携带预览的活动版本 ID、源版本及哈希，不接收主机路径。复制前后和安装锁内重新检查，拒绝预览后内容变化与并发版本切换；显式同版本不同内容仍拒绝。更新来源以用户确认的预览为准，不声称固定原始来源或自动同步。
31. 更新保留禁用状态：目录更新和 ZIP 升级均不隐式启用已禁用的 Skill；显式卸载后重新安装仍允许启用。回归覆盖预览、内容竞争、活动版本竞争、历史保留、禁用保留、跨用户与工作区角色权限。普通目录 Skill 的脚本策略开关已接到详情，不再要求先纳入管理；仍受管理员权限与真实 OCI 条件约束。
32. 工作区工具配置（v38）：平台运营配置先决定 MCP/Hosted 可见范围，工作区管理员只可切换已授权项；公开配置不接受端点、命令、环境变量或向量库 ID。数据库成员权限再次核验，版本化 PATCH 拒绝并发覆盖；旧后端 404 时前端保持只读。目录 Skill 与已固定的目录/管理包执行副本均实时检查工作区工具依赖。
33. 工具配置绑定（v38）：外部调用及审批固定配置哈希，SDK 闭包传递所选哈希，后端在登记和实际开始时核对。端点替换、撤销权限和禁用后重新启用使旧审批失效；v37 迁移保留旧记录，但不替旧审批补造绑定。私有目录由可信活动引用恢复工作区，不能返回其他工作区的配置。
34. 隔离真实验收：新增独立端口、数据库、SDK Session、二进制和 stdio 合成数据目录；启动/停止只操作身份匹配的隔离进程。保留数据重启验证路径已建立，重启保留原配置和历次日志，不使用本地演示服务的 PID、数据或停止脚本。
35. 真实审批缺陷：首轮 MCP 读取真实成功，但 Agent 提交了“等待批准”的普通回复，未登记写工具，未产生审批卡或外部写入。主 Agent 与后台 Agent 增加实际调用以触发 SDK interruption 的规则，主对话普通终态提交在 SDK/Go 两层拒绝待审批工具。新增回归通过；修复后一次真实重试在 agent_loop 失败，历史日志未保留底层异常类型，不能归因为某个确定原因。之后再次执行相同请求生成真实审批，跨服务重启后在前端批准，原 SDK 调用只写入一次并提交成功。
36. Worker 领取鉴权：隔离日志发现状态化执行 Worker 与后台 Skill Worker 的任务领取接口漏带服务鉴权头，被后端持续 401 拒绝。两条领取路径现显式携带鉴权；新增真实本地 HTTP 204 领取回归（不是替换 Backend 方法的 mock）。修复后 Sidecar 全量 274 tests 通过。主对话错误日志增加仅含异常类型链和 HTTP 状态的诊断，不输出密钥、请求正文或原始错误消息。
37. 失败说明可见：对话消息展示服务端公开的失败原因，不再把原生压缩错误隐藏在进度详情里；原请求仍保留。审批卡工具与来源改成完整行，避免窄侧栏长工具名被挤成碎行。前端全量 168 tests 和生产构建通过，实际窄屏复查仍待完成。
38. 文本来源与已有产物：真实后台请求直接提供了合成资料，却被“来源定位为空”统一拦成缺材料。来源定位现在区分本轮文本与必须绑定的已有产物；已有产物缺失、部分匹配或歧义仍要求澄清，且主 Agent 不能自行塞入未被来源定位选中的产物。来源约束随原生 checkpoint 保存，旧 checkpoint 缺字段时保守处理；SDK 全量 281 tests 及后续 checkpoint 回归通过。
39. 后台结构化输出：修复来源后任务已真实创建并领取，但兼容 JSON 路径在 BackgroundTaskOutput 校验处失败；该路径未向模型提供完整 Schema。现补齐与 SDK 结构化输出相同的完整 Schema（包括 artifact_draft.payload），不补造模型缺失字段，保留 Pydantic 校验。结构化输出及审批恢复定向 10 tests 通过；真实同任务重试仍在进行中。日志只增加函数位置和白名单字段校验类型，不记录模型原始输出。

2026-09-06 v38 代码证据：Go 全量 `test -p 1 ./... -count=1` 通过（Runtime 264.090s，HTTP 41.115s），`go vet ./...` 通过；Sidecar 全量 269 tests 通过；前端全量 27 文件 / 166 tests 及最新生产构建通过。之后审批规则及提交保护的 Sidecar 定向 51 tests、Go 审批 checkpoint 回归 9.136s 通过。以上不等于真实集成全部达标。

v38 仅运行于 `.tmp/sdk-acceptance-20260906-034624/`，入口 `http://127.0.0.1:8880/`；本地演示 `8860` 仍是 v37 后端/Sidecar，未重启。隔离失败样本项目 `prj_7c77ad56bf8e958b7ad350471f2cc05f` 保留，修复后相同首轮请求的新项目为 `prj_490b66d0594a782e5e5c18c457f7f38a`。没有连接或部署测试机，独立全局 review 仍未开始。

2026-09-06 真实 MCP 审批证据：项目 `prj_439bcec93b36acc922829d0ec3e948a0`。读取 `tcall_823571fbb003099b5b417e9c26094804` 完成；写调用 `tcall_b666105102985456668235ef410ec765` / SDK `call_OElhGl1my43mzbkdkSb6lsYO` 在审批前与重启后均为 pending_approval，合成输出文件不存在。前端点击允许后，同一调用 completed/approved；`mcp-writes.jsonl` 只有一行 `acceptance_marker=sdk-live-20260906`，没有重新读取或重复写入；终态提交 `tcall_896cb71bd57bdcfe21372d4b701b47a3` 完成。页面完成回复与进度入口的 3 次工具记录匹配。该项证明主对话批准与重启恢复，不代表其他三模式或外部服务已通过。

隔离环境通过正式 ZIP 安装接口安装现有三种 fixture（`story_fact_check`、`story_research_digest`、`story_review_workflow`），首页无需改动即可显示新增入口；安装记录和 ZIP 均只在隔离 data 下，本地演示未安装这些包。后续分别验收 inline、background_task、stateful_workflow。

真实拒绝与取消：拒绝项目 `prj_3ab4055ec9fd1883e7646c5dde6e034a` 中，写调用 `tcall_f017711605f55f9581b2ec900284ea99` 在前端拒绝后保持 rejected，Agent 明确说明未写入。取消项目 `prj_6dff8bc48535cd9cd398160bef03fe23` 从动态菜单选择上传的 inline Skill，真实完成 load_skill_instructions、MCP 读取并申请写审批；点击停止后 Turn `turn_e9b8cf8dac8e32c0b54e2aea812d6093` 与写调用 `tcall_adfa8b134713ff78cbef72a2511a3e82` 均 cancelled，Run/后台 Task 均为 0。两个测试均未增加合成写入。此轮没有发生 handoff，不能据此声称原生 handoff 真实验收通过。

多轮会话仍阻塞：已通过批准的项目后续请求触发 NativeCompactionProtocolError（Turn `turn_acb9eb3e31ba315b10b8d767186f0580`），后端保留会话并取消待审批写入；该项是当前网关协议不达标的真实表现，不能把独立新会话通过当成多轮会话通过。

后台样本：原来源误拦项目 `prj_025a80f8d76cbffd8ea6e697c61c2d64` 保留。修复后项目 `prj_a5911a8078d570ed4e1788948c75bf49` 创建后台 Task `agt_e48a78c4cbd3f098aba910cdcc402e47`，真实结构校验失败及重试历史保留。状态化流程准备项目 `prj_64a985564b5ba2797f3abe2c35ceda0a` 已通过正式上传 API 导入仓库 brief-synopsis.txt，尚未启动；上传凭据与素材 ID 证据仅保存在隔离目录 workflow-upload-evidence.json。

2026-09-06 目录更新证据：Go 全量运行中 Runtime 289.483s、HTTP 65.211s 等通过，四个包被 Windows 拒绝启动测试程序；获准单独补跑四包全部通过，不把首次失败记作全量通过。`go vet ./...` 通过。前端 27 文件 / 162 tests 及生产构建通过；目录更新组件 14 tests。独立浏览器只读检查现有 outline_critic，显示原 `.agents/skills/outline-critic/SKILL.md`、一致版本和哈希，无可提交更新；桌面、390px、820px 均通过，后两者 scrollWidth 分别为 390/820，无横向溢出。

目录更新激活前备份：`.tmp/local-acceptance-backup-20260906-024547/`，data 41 文件及 SDK Session、旧可执行文件共 43 文件均 SHA256 一致。激活后 schema=37、quick_check=ok、foreign_key_check=0，对话/Run/任务/受管理 Skill 状态数量不变。当前进程 Sidecar 15116、Go 22032、前端 21788（再次操作前需核验）。未上传测试包、修改源 Skill 或提交业务任务；独立全局 review 尚未开始。

2026-09-06 v37 证据：Go 全量通过（Runtime 336.809s，HTTP 68.775s）；最后主对话/后台绑定与迁移回归再次通过（Runtime 53.859s，HTTP 24.825s），运行中三个撤销路径通过（9.779s），`go vet ./...` 通过。脚本回归曾被 Windows 拒绝启动 vet.exe，该次不算通过，已用后续获准的本地重跑替代；关闭策略的新增测试首次漏传策略版本，修正测试请求后通过。Sidecar 与前端本批未改代码，不把此前结果记作新运行结果。

本机 v37 激活前备份：`.tmp/local-acceptance-backup-20260906-022020/`，data 39 文件和 SDK Session、v36 可执行文件全部 SHA256 一致。激活后 schema=37、quick_check=ok、foreign_key_check=0；对话 committed=2，Run cancelled=33/completed=18/failed=1/waiting_approval=3，后台任务=0，受管理 Skill=1，均与激活前一致。进程 Sidecar 6956、Go 6440、前端 8628（再次操作前需核验）；独立浏览器页加载和范围控件检查通过，未创建测试 Skill 或业务任务。全量能力验收未完成，独立全局 review 未开始。

2026-09-05 本批最新证据：v35 Go 全量通过（Runtime 233.151s，HTTP 63.627s）；后续作用域选择器两项回归通过；`go vet ./...` 通过。Sidecar 全量 267 tests 通过，前端 27 文件 / 156 tests 及 production build 通过。23:31 本机已加载 v35，quick_check=ok，原有两轮 committed 对话及各状态 Run 数量不变。独立全局 review 未开始。

2026-09-06 v36 最新证据：Go 全量通过（Runtime 327.648s，HTTP 113.106s），最后扫描改动的 Skill 定向回归再通过（Runtime 56.366s，HTTP 17.435s），`go vet ./...` 通过。Sidecar 全量 268 tests；前端全量 27 文件 / 159 tests，末次组件 11 tests 及生产构建通过。首次 Go 全量中两个临时测试程序曾被 Windows 拒绝执行，该次不算全量通过，已用后续完整重跑替代。

本机 v36 激活前备份：`.tmp/local-acceptance-backup-20260906-010917/`，data 37 个文件与 SDK Session、v35 可执行文件均 SHA256 一致。激活后 schema=36、quick_check=ok；对话 committed=2，Run cancelled=33/completed=18/failed=1/waiting_approval=3，后台任务=0，受管理 Skill=1，均与激活前一致。进程 Sidecar 27732、Go 15592、前端 9700（再次操作前需核验）。独立浏览器验收仅切换安装范围和选择现有项目，不提交安装、启停或新任务；页面现在展示真正执行的不可变归档路径。独立全局 review 仍未开始。

本机联调备份：`.tmp/local-acceptance-backup-20260905-213602/`，包含完整 data（27 文件全部 SHA256 对比一致）、SDK Session 和旧 Go 可执行文件；启动脚本改用项目固定的 Sidecar 虚拟环境。没有 push、commit 或部署。

v35 激活前新增一致性备份：`.tmp/local-acceptance-backup-20260905-233055/`，完整 data 的 35 个文件全部 SHA256 校验一致，另包含 SDK Session 与 v34 可执行文件。原备份和 Git 回滚点保留。新版进程为 Sidecar 3220、Go 632、前端 6436（使用前需重新核验），地址保持 127.0.0.1:8860。独立验收页确认原“大纲诊断”仍启用，没有增加演示 Skill。

真实 UI 操作使用原有 `outline_critic@1.0.0`：扫描、纳入工作区管理、禁用、恢复启用均通过；没有新增演示 Skill，原 `.agents/skills/outline-critic/` 未修改。活动副本位于 `data/skills/active/workspace_internal_shared/outline-critic/`，最终保持启用。用户原有浏览器页未导航或重载，验收使用独立的临时页面，浏览器尺寸覆盖已重置。

## 后续复核范围

- 前端操作可达性、状态更新、消息排序、配置表单、任务取消/重试、审批和工具执行记录。
- Skill 安装、资源读取、版本固定、禁用/卸载、工作区隔离和路径安全。
- 主 Agent、后台 Worker、SDK Session、超时/重试/取消/审批恢复。
- HTTP 权限、跨工作区访问、本机访问保护、敏感信息投影。
- 本地测试、构建和浏览器验证；外部网关、真实 MCP/OCI 与远端验收另列，不能用 mock 通过替代。
